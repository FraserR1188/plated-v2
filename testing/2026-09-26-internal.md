# Test session — self (dev pass) — 2026-09-26

**Build / version:** No device run. Found in the read-only receipt-scanner investigation against master @ `177ed0a` (`docs/2026-09-25-receipt-scanner-findings.md`, candidate C), then measured on the live database by Robbie.
**Device & OS:** none
**Format:** internal dev pass
**Goal of this round:** Stop the meal list silently truncating once an account passes the API's row cap.

Everything here was found by the developer, not a tester. Nothing in this doc can reach `Verified` until a tester exercises the flow after the fix ships.

**Numbering.** PL-050 was taken as the next free number: the highest in the README tables and the newest session doc (`2026-09-25-internal.md`) was PL-049.

---

## Issues raised

| ID | Severity | What happened | Status |
|---|---|---|---|
| PL-050 | Major | `fetchEntries` selects every `meal_entries` row in one unpaged request, and PostgREST silently caps a response at `max_rows` | In progress |

---

## Rectification log

### PL-050 — `fetchEntries` truncates silently at the API's row cap

- **Severity:** Major.
  - Nothing errors. Past the cap, the oldest-logged rows just stop reaching the store.
  - Today/History show those days as empty, the 30-day CSV can come out short, and a long-pending plan can miss the confirmation banner.
  - It's the wrong-numbers class, and invisible to the user.
- **Reported as:** Found in code review. The receipt-scanner investigation (`docs/2026-09-25-receipt-scanner-findings.md`, candidate C) noted `fetchEntries` has no range while `supabase/config.toml` sets `max_rows = 1000`.
- **MEASURED 2026-09-26:**
  - User A has 787 rows, at 8.4/day since 2026-06-24.
  - At that rate A crosses 1000 around 2026-10-21, if hosted Max rows = 1000. That setting wasn't confirmed at the time of measuring.
  - **Stopgap, same day:** hosted Max rows raised from 1000 to 2000 in the dashboard (no deploy). User A now crosses around mid-February 2027.
- **Root cause:** Confirmed from the code.
  - `fetchEntries` ran `select("*").eq("user_id", …).order("logged_at", desc)` with no `.range()` ([useStore.ts:638-649](../src/store/useStore.ts) at `177ed0a`).
  - PostgREST answers that with at most `max_rows` rows and no error, so the store held the newest `max_rows` rows as if they were all of them.
  - The order also had no unique tiebreaker. A bundle apply inserts several rows with one `logged_at`, so any paging on that order alone would read tied rows twice or not at all across a page boundary.
- **Fix:**
  - Page with `.range()` until a short page returns.
  - Order by `logged_at desc, id desc`, so every row has one fixed position and pages can't overlap or skip.
  - Page size is `ENTRIES_PAGE_SIZE`, below `max_rows` and pinned there by a test that reads `supabase/config.toml`.
  - A failed page throws inside the pager. `fetchEntries` reports it once and keeps the previous list: a partial list is never installed as if complete (the PL-003 rule).
  - Local `max_rows` set to 2000 to match hosted.
- **Where:** master @ `be8ed92` (red tests + filing) and the fix commit that follows it. Code: `src/lib/paging.ts` (`fetchAllPages`), `src/store/useStore.ts` (`ENTRIES_PAGE_SIZE = 500`, `fetchEntries`), `supabase/config.toml` (`max_rows = 2000`).
- **Local checks (MEASURED 2026-09-26):**
  - **Red at `be8ed92`**, against the unpaged select and a fake capped at 1000 rows:
    - 1000 of 2,350 rows held;
    - 200 tied rows missing;
    - the failed-page case installed 1000 rows as if complete;
    - page-size tests red because the constant didn't exist.
  - **Green after the fix:** the new tests pass 11/11. The full suite passes **1033/1033 across 54 files** (was 1022/52). `tsc --noEmit` reports 0 errors. `entriesCoverage.test.ts` is unchanged and green.
  - **Sabotage 1, `.order("id")` removed:** the shared-timestamp test goes red with `{ duplicates: 4, missing: 4 }`. The 2,350 test goes red at 2,344 unique, and the exact-multiple test goes red on order. Restored, byte-identical, green.
  - **Sabotage 2, pager returns after the first page:** the 2,350 test goes red at 500. The shared-timestamp test goes red with 700 missing. The failed-page test also goes red: a 500-row partial list was installed over the previous one. Restored, byte-identical, green.
  - **Runtime versions** via `npx expo-updates runtimeversion:resolve`: Android `c1907ba4…`, iOS `5359dcce…`. Unchanged, so OTA-eligible.
- **Device checklist (owed, on the Pixel dev build before publishing):**
  - [ ] Today, History and Trends show the same days as before, with a pull-to-refresh on each.
  - [ ] Log a meal, then background and foreground the app: it's still there once and only once.
  - [ ] Settings → Export CSV: the row count matches `select count(*) from meal_entries where user_id = '<A>' and date >= current_date - 29;`.
  - [ ] Airplane mode, then refresh Today: yesterday's list stays on screen and doesn't empty.
- **Sweep of the other per-user selects (not fixed here; candidates, unnumbered):**

  | Read | Where | Pattern | Rows (what's known) | Risk |
  |---|---|---|---|---|
  | `biometric_workouts` | `useStore.ts:691-698` `fetchWorkouts` | Unbounded, **no ORDER BY at all** | Not measured. About one row per workout per source; Health Connect can backfill history (`READ_HEALTH_DATA_HISTORY`) | **Highest.** Past the cap it drops an arbitrary set, not the oldest |
  | `saved_ingredients_scored` | `useStore.ts:896-907` `fetchSavedIngredients` | Unbounded; ordered by `decay_score` only, which ties | 378 rows across all users, as predicted in `verify/20260919120000_verify.sql` (not measured here) | Low. Grows per distinct food. Needs an `id` tiebreaker if it's ever paged |
  | `meal_compositions` + embedded items | `compositions.ts:64-69` `getCompositions` | Unbounded; ordered by `last_used_at`, `use_count` (ties) | Not measured; tens per user at most | Low. PREDICTED: max_rows limits top-level rows, not an embedded list |
  | `follows` (state, friends, requests) | `social.ts:202-212`, `:400-419`, `:449-458` | Unbounded per user | Not measured; small | Low |
  | Friend range | `social.ts:521-529` `getEntriesForUserRange` | Bounded by a date range | — | Only if a caller asks for a very long range |
  | Friend day, today's calories, WHOOP day panel | `social.ts:481-492`, `:763-771`, `whoopScores.ts:96-106` | One day | — | None |
  | CSV export | `SettingsScreen.tsx:571-585` | Reads the store (`getAllEntries`), no query of its own | — | Fixed by this change |

  Single-row reads (`maybeSingle`) and `.limit()` searches (profiles, custom food by barcode, core ingredients, user search limited to 20) aren't affected.
- **Shipped in:** —
- **Verified:** No.
- **Status:** In progress

---

## Deliberately not changed

| ID | Feedback | Why not |
|---|---|---|
| — | The other per-user selects in `src/` | Swept in the same pass; none is fixed here. Listed as candidates in PL-050's sweep table, not numbered |

---

## What went well

- The cap was found in review and measured before any account reached it. Raising hosted Max rows the same day bought about four months without a deploy.
