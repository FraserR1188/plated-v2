# Test session — self (dev pass) — 2026-09-20

**Build / version:** master @ `0f708a8`, no OTA published from this round yet
**Device & OS:** none — source investigation plus read-only production queries (`npx supabase db query --linked`, SELECT-only, counts only). Device passes for the fixes below are still owed.
**Format:** internal dev pass — read-only source investigation (Claude Code), no tester present
**Goal of this round:** scope two pieces of feature work (navigation restructure: Batches → Today header, Insights → centre tab; and WHOOP sleep/strain/recovery beside the calorie ring) and record the defects the investigation turned up on the way.

Everything here was found by the developer, not a tester. Nothing in this doc can reach `Verified` until a tester exercises the flow after the fix ships.

Source: the read-only investigation of 2026-09-20 (WHOOP/Insights scoping). Measurements quoted below were taken against production at 13:22 UTC on 2026-09-19 with the three WHOOP users anonymised as u1–u3; no score values were read, only counts, states and ages.

---

## Issues raised

_Next free PL- number: check the open and closed tables in `testing/README.md`. Numbers are never reused._

| ID | Severity | What happened | Status |
|---|---|---|---|
| PL-018 | Major | Today and its workouts don't refetch when a foreground WHOOP or Health Connect sync resolves | Logged |
| PL-019 | Minor | whoop-sync never closes or deletes cycles WHOOP has dropped; one orphan open cycle (u1, 57.7 days), `is_current` returns 2 rows | Logged |
| PL-020 | Minor | Batches "Logged" alert says "today's log" even when a past day was picked | Logged |
| PL-021 | Minor | Predicted: Today header overflows at 360dp on off-today pages while the Bundles chip shows | Logged |
| PL-022 | Polish | `whoop_data.sql:125-127` documents an upsert gate that the sync doesn't implement | Logged |
| PL-023 | Blocker | Settings' goals form seeds once from the store and saves all eight fields, so any failed goals read persists the defaults over a real row | Fixed |
| PL-024 | Major | Clearing or mistyping a goal field silently saved the hard-coded default; an explicit 0 did too | Fixed |
| PL-025 | Minor | mealEntriesInsertSites.test.ts resolves variables by name file-wide, so an unrelated local named `patch` flips its exemption into the stale branch | Logged |
| PL-026 | Minor | History's adherence % is computed against goals whatever their load state, so an `error` state reports adherence against the defaults | Logged |
| PL-027 | Minor | Today shows the default 2,000 kcal and macro targets as the user's own while goals are in `error` | Logged |
| PL-028 | Major | A product with no nutrition data in OFF is stored as all-zero, and a bundle freezes it; 35 zeroed rows across 2 of Robbie's accounts | Fixed — data repaired 2026-09-20, code device-passed on Pixel |
| PL-030 | Polish | ProductScreen's preview shows 0.0 for a small-four value the insert correctly stores as NULL | Logged |
| PL-031 | Major | The PL-028 repair backup was created in `public`, so Supabase's default grants gave `anon` and `authenticated` full DML on 41 rows of nutrition data and user_ids across 3 accounts, with RLS off | Fixed |
| PL-032 | Minor | "Three bean chilli and avocado salsa" holds two identical 6 g Salt items; possibly a duplicate insert | Logged |

---

## Rectification log

### PL-018 — Today doesn't refetch after a foreground sync

- **Severity:** Major
- **Reported as:** Found in code review, while establishing how stale WHOOP strain can be when Today renders.
- **Root cause:** `App.tsx:313-342` fires `syncWhoop()` and `syncHealthConnect()` fire-and-forget on session set and on every `AppState` → `active`. Nothing observes either promise. `TodayScreen`'s focus effect (`TodayScreen.tsx:231-237`) runs `fetchEntries`/`fetchCompositions`/`fetchWorkouts` at mount and on focus, which happens *before* the sync lands. So the first render after foregrounding shows the previous sync's data, and the rows the sync just wrote aren't read until the next focus or a pull-to-refresh. Nothing re-syncs while the app stays foregrounded, so a long session freezes. Measured staleness of the newest cycle row at 13:22 UTC: 60 min (u1, connection-level), 178 min (u2), ~31 h (u3).
- **Affects:** WHOOP workouts today (the only biometric surface Today reads); it will affect the sleep/strain/recovery panel the same way once that ships.
- **Fix:** `src/lib/syncRefetch.ts` watches each sync promise and re-reads once it resolves, if it wrote anything. The syncs stay fire-and-forget: `runSyncs()` returns immediately and the watchers resolve on their own.
  - **Only workouts are re-read.** whoop-sync writes the `whoop_*` tables and health-connect-ingest writes the `biometric_*` tables; neither touches `meal_entries` or `meal_compositions`, so Today's meals and bundles cannot change from a sync. Refetching them would be an unbounded query per foreground for data that provably didn't move. The caller passes the refetch in, so the WHOOP day-score panel joins that one callback later.
  - **Detecting "wrote nothing", so the common foreground costs nothing:** WHOOP reports `skipped: 'throttled' | 'revoked'` (both return before the pull loop) and per-collection `counts`; Health Connect reports per-domain counts. Both are read precisely. Where the answer is genuinely unknown the code refetches rather than guessing: a WHOOP success carrying no counts, a `transient`/`server_error` failure (a collection can fail on page 3 having already upserted pages 1-2, `whoop-sync/index.ts:355-360`), and a Health Connect domain that threw mid-pass. Failures decided before any write (`network_error`, `unauthorized`, `not_connected`, `revoked`, the auth-only codes) skip it.
  - **Health Connect deletions are now counted.** `HealthConnectSyncResult` gains `deletions`, kept separate from `counts` so Settings' "N records synced" is unchanged. Without it a deletion-only pass — a workout deleted in another app arrives as a deletion with zero upserts — was indistinguishable from "nothing happened", and Today kept rendering a workout the database no longer had.
  - **Cost:** at most two refetches per foreground (one per sync that wrote), deliberately not coalesced — WHOOP's pull can take tens of seconds while Health Connect's is local and quick, so making the fast one wait would delay the data reaching the screen, and reusing one in-flight refetch can only return rows read before the second sync's write.
  - **PL-014 (no double report):** a rejected sync is not reported by the watcher — the existing `.catch` at the call site owns that. The store's fetch actions report their own query failures internally and resolve rather than throwing, so the watcher's own catch only fires for an error that escaped unreported, under `todayRefetchAfterSync`.
- **Where:** `<this commit>` — `src/lib/syncRefetch.ts` (new), `src/lib/healthConnectSync.ts`, `App.tsx:315-356`
- **Tests:** `src/lib/__tests__/syncRefetch.test.ts` (18, red first — the module didn't exist), plus a deletion-only case in `healthConnectSync.test.ts`. Sabotage runs: deleting the `skipped` check fails 3 tests; dropping the deletion count fails the deletion test; removing the refetch catch fails the report test. The first sabotage initially PASSED and exposed two vacuous tests — the throttled fixture carried all-zero counts, so it returned false via the wrong branch. Real throttled responses carry no counts at all, and the fixtures now match that.
- **Shipped in:** OTA, production channel, 2026-09-20, master @ `2f8195f`. Android group `10b5ab16-ef93-4585-b5f9-9a2f6c4bfae1` (runtime `c1907ba4…`); iOS group `7f5a5b0b-0e2a-4ebc-996f-0f7860426993` (runtime `5359dcce…`).
- **Verified:** Own device pass, reported by Robbie 2026-09-20: passed on **Pixel and iOS**. Cold start showed workouts without a pull-to-refresh, a foreground inside 15 minutes made no extra call, and data updated after 15. Not yet exercised by a tester, so this stays `Fixed`, not `Verified`.
- **Status:** Fixed

### PL-019 — Dropped WHOOP cycles are never closed or deleted

- **Severity:** Minor
- **Reported as:** Measured on production while checking which cycle maps to "today".
- **Root cause:** `supabase/functions/whoop-sync/index.ts` only ever upserts (blind upserts, `:290-297`); it has no delete or close path (`grep` for `.delete(`/`delete from` returns nothing). A cycle WHOOP later drops or re-scopes stays in `whoop_cycles` with `"end" IS NULL` forever, because the rolling 7-day window (`:492-504`) stops re-pulling it. Measured: u1 has **two** rows with `end IS NULL` — one open 0.8 days (genuine, current) and one open **57.7 days**, last written 81,741 minutes ago. Both surface as `is_current = true` in `biometric_periods_resolved`, so that user has 2 "current" frames.
- **Blast radius today:** contained. `whoop_cycle_nutrition`'s 36-hour guard gives a stale open cycle `effective_end = NULL`, so it owns no meals, and `prev_cycle_contiguous` is false for its successor. The orphan's wake-anchored `local_date` sits 57.7 days in the past, so a date-keyed read is unaffected.
- **Constraint going forward:** **nothing may select "today's cycle" via `is_current`** — including the LLM readiness query. `is_current` is not unique per user and is not a date. Select by wake-anchored `local_date` instead (`biometric_periods_resolved.local_date`).
- **Fix:** _not scheduled. Options: a reconciliation pass that closes or deletes cycles absent from a re-pulled window, or accept and rely on the date-keyed reads plus the constraint above._
- **Status:** Logged

### PL-020 — Batch log alert always says "today's log"

- **Severity:** Minor
- **Reported as:** Found in code review.
- **Root cause:** `BatchesScreen.tsx:103-105`. The eat-time sheet lets the user pick any day, and the branch is on `willBePlanned(chosenAt)` alone: anything not in the future falls to `Alert.alert("Logged", \`${batch.name} added to today's log.\`)`. Picking yesterday 12:00 logs correctly against yesterday but reports "added to today's log".
- **Fix:** New pure helper `batchLogAlert` (`src/lib/batchLogMessage.ts`) builds both the title and the body from the picked instant. The day comes from `formatDayLabel(dateKey(chosenAt))`, so it reads "today's log" only when the pick really is today, "yesterday's log" for yesterday, and "added to the log for Sat 12 Sept" for anything older. The planned branch still mirrors the DB trigger through `willBePlanned`, grace included, so the alert can't contradict what was stored. `BatchesScreen` just renders what the helper returns.
- **Where:** `<this commit>` — `src/lib/batchLogMessage.ts` (new), `src/screens/BatchesScreen.tsx:95-101`
- **Tests:** `src/lib/__tests__/batchLogMessage.test.ts` (7, red first). Sabotage: hardcoding "today's log" again fails 2. One expectation of mine was wrong on first run — Intl's en-GB short month for September is "Sept", not "Sep" — so the test was corrected to match the formatter, not the other way round.
- **Shipped in:** OTA, production channel, 2026-09-20, master @ `2f8195f`. Android group `10b5ab16-ef93-4585-b5f9-9a2f6c4bfae1` (runtime `c1907ba4…`); iOS group `7f5a5b0b-0e2a-4ebc-996f-0f7860426993` (runtime `5359dcce…`).
- **Verified:** Own device pass 2026-09-20, then **re-confirmed in the combined pass** after Batches returned to the tab bar (the alert is reached from the Batches tab again, not a pushed screen): logging against today, yesterday and an older day each named the right day, and a future pick still read as planned. Not yet exercised by a tester, so this stays `Fixed`.
- **Status:** Fixed

### PL-021 — Today header overflows at 360dp off-today

- **Severity:** Minor
- **Reported as:** Predicted, not yet seen on a device. Computed from the bundled font files (glyph advance widths from `@expo-google-fonts/hanken-grotesk`), not from a render.
- **Root cause:** The header row (`TodayScreen.tsx:804-858`) puts "Return to today" (104.4dp incl. padding and border), the Bundles chip (65.6dp) and the 36dp calendar button in one right-hand group with 6dp gaps — 218dp of a 328dp row at 360dp width. That leaves 110dp for the left block, and the anchor "Wednesday" measures 123.3dp, so the longest weekday wraps. On today's page the same combination fits (220.4dp available, 183.6dp needed for the longest date line).
- **Why it matters now:** adding a Batches button to this row makes it worse, so the reflow is part of the navigation work rather than a follow-up.
- **Fix:** Header reflow. The right-hand row is now three fixed 36dp icon buttons (Bundles when bundles exist and not selecting, Batches, Calendar) — 120dp including gaps at any width. "Return to today" left that row entirely and took the eyebrow's place on off-today pages as a tappable "‹ Today", which costs no extra row height. Measured from the bundled font files at 360dp: the left block is 208dp, against 123.3dp for the longest anchor ("Wednesday") and 183.6dp for the longest date line ("Wednesday 30 September" plus its status dot) — the tightest of the two with 24.4dp spare. Also fits at 375 (iOS floor, 39.4dp spare) and 412. The anchor and date line gained `numberOfLines={1}` + `adjustsFontSizeToFit`, because that 24.4dp runs out at roughly a 1.14x system font scale and shrinking beats wrapping.
- **Where:** `<this commit>` — `src/screens/TodayScreen.tsx` header block and styles
- **Tests:** none — this is layout, and the arithmetic above is font-metric measurement, not a render. It needs the device pass to become a fact.
- **Shipped in:** OTA, production channel, 2026-09-20, master @ `2f8195f`. Android group `10b5ab16-ef93-4585-b5f9-9a2f6c4bfae1` (runtime `c1907ba4…`); iOS group `7f5a5b0b-0e2a-4ebc-996f-0f7860426993` (runtime `5359dcce…`).
- **Verified:** Own device pass, reported by Robbie 2026-09-20: passed, at both densities. The font-metric arithmetic above is now backed by a real render. Not yet exercised by a tester, so this stays `Fixed`.
- **Reflow revised, same day (design review):** the icon-only Bundles and Batches buttons didn't read as anything, so the row is now the **labelled "Bundles" chip plus the 36dp calendar** and Batches went back to the tab bar. What actually fixes PL-021 is unchanged and still in place: "Return to today" living in the eyebrow rather than the icon row. Re-measured at 360dp — chip 65.6 + gap + calendar 36 = 107.6dp, leaving **220.4dp** against the same 183.6dp worst-case date line (36.8 spare, ~x1.17 font scale; 375dp: 51.8 spare; 412dp: 88.8). Needs the combined device pass below to re-confirm.
- **Verified (revised reflow):** combined device pass 2026-09-20 — **Pixel at default density, at 360dp (`wm density 480`) and at the largest system font**, today and off-today, with and without bundles, and in selection mode: no wrap and no overflow, and "‹ Today" returns to today. **iOS** checked at the largest Dynamic Type. The font-metric budget above is now backed by a render on both platforms.
- **Status:** Fixed

### PL-022 — Schema comment describes an upsert gate that doesn't exist

- **Severity:** Polish (documentation only)
- **Reported as:** Found in code review.
- **Root cause:** `supabase/migrations/20260712120000_whoop_data.sql:125-127` says `whoop_updated_at` is "the upsert GATE: only write if theirs is newer than ours". The sync does the opposite and says so: "Upserts are BLIND — no read-then-compare against `whoop_updated_at`" (`whoop-sync/index.ts:290-297`). The code is right and the comment is stale; the practical consequence is that every row's `synced_at` moves on every sync, which is what makes `synced_at` usable as a per-row "last synced" signal.
- **Fix:** _not scheduled. A comment correction on the migration is not worth a migration; fix it in place if that file is edited for another reason, or leave the code comment as the authority._
- **Status:** Logged

---

## Design change — 2026-09-20, after review

The nav restructure shipped in this round was revised before publishing, on review of the built screens:

- **Batches returns to the tab bar** with its own in-screen header and "+ New". Reaching it through a header icon on Today was not discoverable — neither the Batches nor the Bundles glyph read as anything.
- **Friends leaves the tab bar** for a row in Settings (directly after Sharing), pushed as a root-stack screen with the native header. Its pending-request badge shows in both places — on the Settings tab and on the row — from the same store count, so they cannot disagree.
- Tabs are now Today, History, **Insights** (centre), Batches, Settings. `SOCIAL_ENABLED` moved from TabBar to `src/lib/flags.ts` because it now hides a Settings row rather than a tab; as a result it no longer changes the tab count, so Insights stays centred whatever the flag says. That removes the caveat noted when Insights shipped.
- Kept from the first attempt: the PL-020 alert fix, "‹ Today" in the eyebrow, the typed icon map, and the Insights stub.

**One behaviour change fell out of it.** With Friends pushed from Settings, `CopyConfirm`'s `popToTop()` would have landed a friend-copy on the Settings tab. It now lands on **Today, showing the day the copy went to** (`setViewedDate` then `popTo`) — see PL-001 below.

### Combined device pass — 2026-09-20, passed

One pass over the whole restructure, replacing the two that were pending. Reported by Robbie.

- **Pixel**, at default density, at 360dp (`adb shell wm density 480`) and at the largest system font:
  - Tab bar: Insights centred at 360 and 412; the Batches tab works end to end (New → editor → save; recipe scan → confirm; Log and Plan, with the alert naming today, yesterday and an older day correctly).
  - Today header: today and off-today, with and without bundles, and in selection mode — no wrap, no overflow; "‹ Today" returns to today.
  - Settings → Friends → back via the on-screen button, hardware back and the back gesture; the badge shows on the Settings tab and on the row, and clears once requests are handled.
  - Copying a friend's single ingredient, a meal section and a full day all land on Today showing the target day, including a target that isn't today, and back does not re-enter Friends or CopyConfirm. PL-001's sat fat still copies.
- **iOS:** header at the largest Dynamic Type, the Friends native header, and CopyConfirm's modal offset reached from inside Friends.

**Fingerprint, measured on both platforms:** `c1907ba4619df4d22e128f15df609529906eae2a` (Android) and `5359dccea82b8462c0d3d81501d9748bbb241baf` (iOS), identical at `0f708a8` (before this work), at `04e183a` (the restructure) and at `07c6af1` (the stub copy change that ships with it). The whole round is `src/`-only, so it reaches installed builds as an OTA.

### Insights stub — copy change, 2026-09-20 (pre-publish)

Not a defect; a wording change made before this work shipped, recorded so the published copy has a trail.

- **Eyebrow added:** "Coming soon", above the heading, in the same treatment Today's header eyebrow uses (xs, medium, muted), grouped with the heading so it sits 2dp above it rather than taking the centre stack's 8dp gap.
- **Heading:** "Insights unlock after 31 days of tracking" → "**Insights will unlock after 31 days of tracking**".
- **Body unchanged:** "We need about a month of your meals to find patterns worth showing you."
- **Still fully static** — no store reads, no queries, no counter. A test now asserts that as well as the wording (`src/screens/__tests__/insightsStub.test.ts`, 4 tests, red first; no test asserted the copy before). Sabotage: dropping the eyebrow, reverting the heading, and importing the store each fail exactly one test.
- **Fingerprint unchanged:** `c1907ba4…` / `5359dcce…` on Android/iOS — `src/`-only change, as expected.

---

### PL-023 — A failed goals read makes Settings persist the defaults over real targets

- **Severity:** Blocker (silent data loss — the user's own saved targets)
- **Reported as:** Found while answering a read-only challenge to the PL-017 fix: under RLS a query run without a valid session returns **zero rows, not an error**, so `maybeSingle()`'s no-row branch cannot tell "this user has no goals row" from "that read didn't carry a session". Chasing whether anything could then *write* the defaults back turned up a path that does.
- **Root cause — three parts, all of which already existed:**
  1. **The form seeds once and never re-syncs.** `SettingsScreen.tsx:129-138` builds `values` in a `useState` **initialiser** from the store's `goals`. `goals` is destructured from the store so the component re-renders when it changes, but `values` does not follow it — the only `setValues` in the file is `handleChange` (`:491`). There is no effect syncing the two.
  2. **Save sends all eight fields, not the changed one.** `handleSave` (`SettingsScreen.tsx:495-505`) passes a complete `Goals` object, each field falling back to the same literal defaults (`|| 2000`, `|| 150`, …).
  3. **The write is a full-row upsert.** `saveGoals` (`useStore.ts:1452-1471`) upserts all eight columns keyed on the `user_id` primary key. There is no partial update anywhere — this is the **only** write to `public.goals` in the codebase (`grep` for `from("goals")` returns exactly two sites: this and the read).
- **The sequence:** anything that leaves the store holding `DEFAULT_GOALS` while the user *does* have a real row → the user opens Settings and sees 2000 kcal instead of their targets → they correct one field and press Save → all eight are upserted → **their real targets are gone, with no error and no undo.**
- **How the store ends up holding the defaults — the common route needs no RLS subtlety at all:** any failed `fetchGoals` does it. A network failure at launch reports to Sentry and leaves `DEFAULT_GOALS` in place, and nothing retries or tells the user the screen they are about to edit is showing placeholder values.
- **The RLS route, measured:** `_getAccessToken` falls back to the **anon key** when `getSession()` yields null (`@supabase/supabase-js/src/SupabaseClient.ts:570-578`), and the token is resolved per request inside `fetchWithAuth` (`src/lib/fetch.ts:42-52`), so such a request reaches PostgREST as the `anon` role. Confirmed against production, read-only: `anon` holds **7 privileges** on `public.goals` (SELECT among them), and all three SELECT policies are `(auth.uid() = user_id)` with `polroles` **null** — i.e. `PUBLIC`, so they apply to `anon`. With `auth.uid()` NULL the qualifier is NULL, not true: **zero rows, HTTP 200, no error.** The read is silently empty rather than failing.
- **Not introduced by the PL-017 fix, but the fix widens the window.** Before it, `.single()` turned an empty read into PGRST116 and the handler's `if (data)` left the store alone — which preserved *already-loaded* goals. The new explicit `if (!data) set({ goals: DEFAULT_GOALS })` clobbers them. On the cold-start path the outcome is identical either way (state starts at `DEFAULT_GOALS`), so the write-back hazard is fully reachable on both sides of the fix; what changed is that it is now also reachable **after** a successful load. See PL-017.
- **Blast radius:** one row per affected user, overwritten in place. `goals` has no history, no `deleted_at` and no audit trail, so the previous values are unrecoverable from the database. Users who never set targets are unaffected — the defaults are already their state.
- **Fix — goals carry an explicit load state.** `goalsState: "loading" | "loaded" | "absent" | "error"` (`useStore.ts`), because `DEFAULT_GOALS` in `goals` is indistinguishable from a user whose targets happen to be the defaults, and every consumer — Settings most of all — was reading it as if it were the truth. `loaded` and `absent` are known good and may be written from; `loading` and `error` may not.
  - **Session gate on the read.** `fetchGoals` now calls `supabase.auth.getSession()` and returns `error` without querying when there is no session. This is what closes the anon-key hole: an empty result is only believable if the read actually carried the user's JWT. Not reported to Sentry — a missing session is a lifecycle state, not a fault.
  - **A failed read never downgrades a known-good state.** If `goalsState` is already `loaded` or `absent`, a failure keeps both the goals and the state. Only a failure with nothing known (cold start) gives `error` + `DEFAULT_GOALS`, and those defaults are explicitly display-only.
  - **`absent` is not an error (PL-017).** An empty read *with* a session sets `absent` + `DEFAULT_GOALS` and leaves a Sentry **breadcrumb**, not an event — `noteBreadcrumb()` in `src/lib/reportError.ts`, at `level: "info"` so it can't be misread as a failure. A later error now carries "goals: no row for user" in its trail.
  - **Writes are split by state.** `loaded` → `UPDATE` **only the columns that actually changed**, so a field the user never touched is never written, even from a stale form. `absent` → `INSERT` the full row, then `loaded`. Deliberately an insert and not an upsert: if a row does exist after all, the unique violation is the correct outcome rather than an overwrite. `loading`/`error` → refused with a message, no query.
  - **A zero-row UPDATE is a failure.** An `UPDATE` filtered out by RLS returns no PostgREST error — the `USING` clause just empties the target set and the request "succeeds". `.select("user_id")` plus a zero-length check, the same trap already handled in `saveCompositionApplyQuantities`.
  - **Settings re-syncs, and can't save what it doesn't know.** `values` follows `goals` in an effect instead of a once-only `useState` initialiser, guarded by a `dirty` flag so a late read never overwrites what someone is typing. Save is disabled unless the state is writable, and `handleSave` returns early too — a screen is a bad place for the only copy of a data-integrity rule. In `loading`/`error` the card shows "Couldn't load your targets" and a Retry, theme tokens only.
  - **Foreground retry.** `App.tsx`'s existing `AppState` → `active` listener re-runs `fetchGoals` **only while in `error`**, so the common case (no signal at launch, signal a minute later) fixes itself without the user finding Retry. `loaded` and `absent` are not re-read: that would be an unbounded query per foreground for data that cannot change from this device.
- **Behaviour deliberately unchanged:** a user with no row still runs on `DEFAULT_GOALS` (P-TF01b, still open), and Today's ring and History's adherence still render from `goals` whatever the state. Both are display-only (audited: `TodayScreen.tsx:717-718`, `:754-775`, `:911-1024`; `HistoryScreen.tsx:118-123`, `:240-241`) — no other consumer reads goals at all, `csv.ts` never touches them, and no lib module or Edge Function does either.
- **Where:** `<this commit>` — `src/store/useStore.ts` (`GoalsState`, `fetchGoals`, `saveGoals`, initial state, `reset`), `src/lib/reportError.ts` (`noteBreadcrumb`), `src/screens/SettingsScreen.tsx`, `App.tsx`, `vitest.setup.ts` (`auth.getSession` mock).
- **Tests:** `src/store/__tests__/useStore.test.ts` — 17 across `fetchGoals`/`saveGoals`, red first (15 of 17 failed; the two that passed were the pre-existing no-user exits, kept to pin them). Nine sabotage runs, each discriminating: removing the session gate (1), empty read → `error` (2), failure after a good load clobbering the store (2), dropping the write-state guard (2), writing all eight columns instead of the changed ones — the original bug — (2), treating a zero-row UPDATE as success (1), `absent` not promoting to `loaded` (1), `reset()` leaving the state (2), removing the breadcrumb (1).
- **A structural test caught something on the way.** `mealEntriesInsertSites.test.ts`'s "uninspectable update payloads is exactly {updateEntry}" went RED — not because of anything about `meal_entries`, but because its `resolvesToObjectLiteral` helper looks variable declarations up **by name across the whole file**. Naming a local `patch` inside `saveGoals` made `updateEntry`'s `{ ...patch }` (a spread of its *parameter*) resolve to my unrelated `{}` and appear traceable. Renaming mine to `changes` restored it. **The test is right to be loud, but it is weaker than it reads:** any future local named `patch` anywhere in `useStore.ts` silently converts that exemption into the "stale exemption, delete this test" branch, which is the opposite of what it intends. Worth a scope-aware resolver, logged here rather than fixed in this PR.
- **Sentry search for exposure — please run:** `operation:fetchGoals !error_code:PGRST116`. **Every one of those events is a launch where the store sat on `DEFAULT_GOALS` with no way to tell.** That is the best available measure of how many users could have been exposed to this, and the count, distinct users and first/last seen are all worth having. Note the two caveats from PL-017: this needs an **event-level** (Discover) query because the issue fingerprint is `[operation]` and groups all `fetchGoals` failures into one issue, and **distinct users will come back empty** until the per-install id ships (see the design decision above). A floor either way: reads that failed before `reportError` was wired in (before `a6095cf`, 2026-08-12) are not in Sentry at all.
- **Shipped in:** OTA, production channel, 2026-09-20, master @ `5f347c3`. Android group `faa10722-93cb-4cc2-878b-b5bbcde64c15` (runtime `c1907ba4…`); iOS group `75060c2b-bc04-46b8-8453-2813afe53a52` (runtime `5359dcce…`). Fingerprints measured unchanged on both platforms before publishing, so this reached installed builds as an OTA.
- **Verified:** Own device pass, reported by Robbie 2026-09-20: **passed on Pixel**, all nine checks. Airplane mode at cold start gave the error state with Save disabled, and going online then Retry/foreground brought the real targets back. **A single-column write was confirmed in SQL** — editing one target changed that column and no other, which is the assertion this whole fix exists to make. A no-row account showed the defaults and filed no Sentry event. **`"Loading your targets…"` was too fast to observe online**, and the failure copy appeared only offline, which is the intended split (PL-024). **iOS not run this round — owed**, to be covered by the next iOS pass or tester session. Not yet exercised by a tester, so this stays `Fixed`, not `Verified`.
- **Status:** In progress

---

### PL-024 — Clearing a goal field silently saved the default

- **Severity:** Major
- **Reported as:** Flagged while building PL-023 as pre-existing and out of scope, then pulled into the same PR once it was clear PL-023 had made it worse.
- **Root cause:** `SettingsScreen.handleSave` parsed every field as `parseInt(values.x) || <default>`. That is three silent substitutions in one expression — a **cleared** field (`NaN`), a **typo** (`NaN`), and an explicit **0** (falsy) all became the hard-coded default — plus `parseInt`'s own truncation on top: `"150abc"` is `150`, and `"2,500"`, a perfectly reasonable way to type a calorie target, is **2**.
- **Why it got worse, not better, with PL-023.** While Save blind-upserted all eight columns from a form seeded at mount, a substituted default was one of eight values going over the row anyway. PL-023 writes **only the columns that changed** — and clearing a field *is* a change. So the substituted default became precisely and only what the write targets: the user clears their fibre goal, and 30 g is written to that one column as if they had chosen it.
- **Fix:** New pure module `src/lib/goalInput.ts`. `parseGoalField` refuses empty, whitespace, non-numeric, trailing junk, thousands separators and negatives, and returns a message per field instead of a value. `parseGoalValues` is all-or-nothing by construction: either a complete `Goals` or a map of per-field errors, with **no partial result**, because a partial result is exactly what a write path would be tempted to use. `handleSave` takes `parsedGoals.goals` or returns; nothing is substituted anywhere on the write path.
- **Which fields accept 0, and why:**
  - **All seven macros accept 0.** "No added sugar" and "as little salt as possible" are real targets, and `|| <default>` made them unreachable — typing 0 for sugar saved 30 g.
  - **Calories rejects 0**, with "Must be at least 1". A zero calorie goal isn't a target, it's a division by zero, and the codebase already agrees: both `TodayScreen`'s `frac()` (`goal > 0 ? … : 0`, `:91-92`) and `HistoryScreen`'s ring fraction (`:240`) special-case it.
- **Decimals follow the column type, measured rather than assumed.** Production `information_schema`: `calories`, `protein`, `carbs`, `fat` are `integer`; `sat_fat`, `salt`, `fibre`, `sugar` are `numeric` with no declared scale. So the first four reject a decimal ("Whole numbers only") rather than letting Postgres round `150.5` on the way in and hand back a number the user never chose; the other four accept one. Note this contradicts `schema.legacy-v2.LEGACY-DO-NOT-USE..sql`, which declares `salt numeric(4,1)` and `fibre`/`sugar` as `integer` — another reason not to read that file as current.
- **UI:** each row shows its own message under the input and turns the input border `Colors.danger`; Save is disabled unless the store is writable **and** every field parses (`canSaveGoals = goalsWritable && goalsValid`). Theme tokens only.
- **Where:** `<this commit>` — `src/lib/goalInput.ts` (new), `src/screens/SettingsScreen.tsx`
- **Tests:** `src/lib/__tests__/goalInput.test.ts` (16, red first — the module didn't exist) plus the write-gate assertions in `src/screens/__tests__/settingsGoalsCopy.test.ts`. Sabotage runs: substituting a default on empty fails 4; loosening the parse to `parseFloat` fails 2 (`"150abc"`, `"2,500"`); restoring falsy-zero fails 2; allowing decimals on integer columns fails 1; restoring `parseInt || <default>` in `handleSave` fails 2; gating Save on `goalsWritable` alone fails 1. **One test caught itself**: the "no parseInt-or-default anywhere" assertion first failed against my own comment *describing* the bug, so it now matches comment-stripped source — a test that passes by forgetting why it exists is worth nothing.
- **Shipped in:** OTA, production channel, 2026-09-20, master @ `5f347c3`. Android group `faa10722-93cb-4cc2-878b-b5bbcde64c15` (runtime `c1907ba4…`); iOS group `75060c2b-bc04-46b8-8453-2813afe53a52` (runtime `5359dcce…`).
- **Verified:** Own device pass, reported by Robbie 2026-09-20: **passed on Pixel**. Clearing a target disabled Save with an inline error, an explicit 0 saved as 0, and the rejected shapes behaved — `"2,500"`, `"150abc"` and a decimal on an integer column each refused rather than silently writing a number. **iOS not run this round — owed.** Not yet exercised by a tester, so this stays `Fixed`.
- **Status:** Fixed

### PL-025 — The structural update-site test resolves variables by name across the whole file

- **Severity:** Minor (test infrastructure)
- **Reported as:** Found when it went red during PL-023 for a reason that had nothing to do with `meal_entries`.
- **Root cause:** `mealEntriesInsertSites.test.ts`'s `resolvesToObjectLiteral` → `findVariableDeclaration(sourceFile, expr.text)` looks a declaration up **by name, anywhere in the file**, with no scope awareness. `updateEntry` builds its payload as `{ ...patch }`, a spread of its own **parameter**, which is what makes it legitimately uninspectable. Naming an unrelated local `patch` inside `saveGoals` made that spread resolve to *my* `{}` and appear traceable, so the exemption set went empty and the test failed.
- **Why it matters:** the test's own comment says an empty set means "updateEntry stopped spreading `patch` — the real fix landed, DELETE this test". A name collision produces exactly that signal without the fix, so the documented response to the failure is wrong in this case. The next person could delete a deliberate guard. (Renaming the local to `changes` restored it for now.)
- **Fix:** _not scheduled. Needs a scope-aware resolver — walk up from the reference to the nearest enclosing scope that declares the name, and treat a parameter as unresolvable, rather than scanning the file. Until then, do not name a local `patch` in `useStore.ts`._
- **Status:** Logged

### PL-026 — History's adherence % is computed against goals whatever their load state

- **Severity:** Minor
- **Reported as:** Found while auditing every consumer of `goals` for PL-023.
- **Root cause:** `HistoryScreen.tsx:118-123` computes "days within ±10% of goal" from `goals.calories` unconditionally. PL-023 added `goalsState`, but this screen doesn't read it — so in `error` (a failed read, or a read with no session) it reports an adherence percentage measured against `DEFAULT_GOALS`' 2000 kcal and presents it as the user's own. It writes nothing, so it is not the PL-023 data-loss class, but it is a number the user could act on that is quietly about someone else's targets.
- **Fix:** _not scheduled. Show "–" unless `goalsState` is `loaded` or `absent`._
- **Constraint going forward — alongside PL-009's "don't compare the small four as text in SQL" and PL-019's "never select today's cycle via `is_current`":** **Insights may only compute against goals when `goalsState === "loaded"`.** Not `absent` — `absent` means the user has no targets and the store is showing `DEFAULT_GOALS`, so a "you hit your protein goal 5 days running" built on it is an assertion about a target the user never set. `absent` is fine for *display* (that is what Today's ring does) and wrong for *analysis*. The distinction is the whole reason `goalsState` has four values rather than a boolean.
- **Status:** Logged

---

### PL-027 — Today presents the default targets as the user's own while goals are in `error`

- **Severity:** Minor
- **Reported as:** Found on the PL-023 device pass, 2026-09-20, while checking the offline error state.
- **Root cause:** `TodayScreen` renders the calorie ring, "Left"/"Over", the remaining-calorie number and all seven macro bars straight from `goals` (`:717-718`, `:754-775`, `:911-1024`) with no reference to `goalsState`. PL-023 made the store honest about whether it knows the targets, but only Settings reads that. So in `error` — a failed read, or a read that ran with no session — Today shows 2,000 kcal and the default macro bars with nothing to say they are placeholders, and a user who has set 2,600 sees a "400 left" that is about somebody else's target.
- **Not the PL-023 class:** display only, writes nothing, and PL-023 already blocks the write path that turned this into data loss. What remains is a number the user could act on — eat to a 2,000 kcal ring that isn't theirs.
- **Scope:** `error` only. **`absent` keeps the defaults as they are** — a user with no targets set genuinely runs on `DEFAULT_GOALS` (P-TF01b), and showing "–" to them would be hiding the only numbers they have.
- **Fix:** _not scheduled. Show "–" for the ring total and the macro goals, or a short "targets unavailable" hint, when `goalsState === "error"`. Same family as PL-026, and the two are worth doing together — one pass over the consumers of `goals` that applies the same rule to Today's ring and History's adherence._
- **Status:** Logged

---

## Constraint — WHOOP ids are unique PER USER; every join needs `user_id`

Filed alongside PL-009's "don't compare the small four as text in SQL", PL-019's "never select today's cycle via `is_current`", and the `source_updated_at` constraint below.

`whoop_cycles`' primary key is **`(user_id, id)`**, not `id`. The same WHOOP cycle id can belong to two different users, and on production it does: id `1806788975` is held by both `4dbf04ae…` and `a8435663…`. The same applies to the other `whoop_*` tables, which are keyed the same way.

**So every join between `whoop_*` tables, and every ad-hoc query against them, must include `user_id`.** A join on `id` alone does not fail — it silently produces a cross product between users, which is worse than an error because the result still looks like data.

- **RLS protects the app.** Each table is filtered to the signed-in user *before* any join, so a client read cannot mix users.
- **Service-role and SQL-editor queries are not protected**, and that is where this bites. It is also where verification SQL runs, which is how a wrong join gets to invalidate the evidence for a change rather than the change itself.
- **All four views were audited on 2026-09-20 and every one of their joins already includes `user_id`** — `biometric_periods` (2 joins), `biometric_periods_resolved` (12), `whoop_correlation` (3), `whoop_cycle_nutrition` (1). Eighteen joins, all correct. No issue logged: nothing shipped is wrong.

**How it was found, which is the part worth keeping.** PR 6's own justification was measured with a join on `id` alone. It reported "2 of 172 rows diverge by up to 17.6 hours" when the truth is 116 rows and zero divergence. The dry run caught it because one of the two fabricated drifts was **negative**, and `GREATEST(c, r, s)` can never be less than `c` — an impossible number, not a suspicious one. A plausible wrong number would have survived.

---

## Constraint — `source_updated_at` is a whole-FRAME signal, never strain's time

Filed alongside PL-009's "don't compare the small four as text in SQL" and PL-019's "never select today's cycle via `is_current`", because it is the same kind of trap: a column whose name suggests one thing and whose definition says another.

`biometric_periods` defines it as `greatest(c.whoop_updated_at, r.whoop_updated_at, s.whoop_updated_at)` — cycle, recovery **and** sleep — described in the migration itself as a "freshness signal across all three joined sources". `biometric_periods_resolved` passes it through unchanged.

**So it answers "when did this WHOOP frame last change?", not "when was this number calculated?"** It is a whole-frame signal **by definition**, whatever the data happens to look like. Presenting it under strain would show the sleep scoring time whenever sleep was scored later than the cycle — which does not happen on current data, and which nothing prevents.

**Measured on production: 116 WHOOP rows, ZERO divergence** — `source_updated_at` equals the cycle's own `whoop_updated_at` on every row, and across all 116 cycles neither recovery nor sleep is ever newer than its cycle. So the two coincide on all current data.

**An earlier version of this record claimed 2 of 172 rows (1.2%), up to 17.6 hours. That figure was wrong and is withdrawn.** It came from joining `whoop_cycles` on `id` alone. The primary key is `(user_id, id)` — WHOOP cycle ids are unique PER USER, and two users genuinely share id `1806788975` — so the join cross-multiplied them, inflating 116 rows to 172 and manufacturing a symmetric ±1,053.5-minute "drift" out of one user's frame timestamp against another user's cycle. **PR 6's dry run caught it**, because one of the two drifts was NEGATIVE and `GREATEST(c, r, s)` can never be less than `c`. The impossible number was the tell.

- **Never attach it to a single metric** — not strain, not recovery, not sleep. It belongs to the row.
- **The readiness query must not use it as a per-metric recency or staleness input.** A "your strain data is N hours old" derived from it is correct only for as long as WHOOP keeps bumping the cycle whenever it rescores recovery or sleep. Nothing enforces that, and the failure would be silent. Use `strain_updated_at` (PR 6).
- Per-metric timestamps need their own columns. PR 6 adds `strain_updated_at`.
- The Health Connect arm of the resolved view selects `null::timestamptz` for it, so it is absent for HC-sourced periods entirely.

---

## PR 6 — `strain_updated_at` on the biometric views, and the caption

**Migration:** `supabase/migrations/20260920140000_strain_updated_at.sql`, applied to production 2026-09-20.
**Verification:** `supabase/migrations/verify/20260920140000_verify.sql` — a separate file, following the convention the migrations from `20260901100000` onward use. This started out inline in the migration and was moved once that convention was spotted.
**Caption shipped in:** OTA, production channel, 2026-09-20, master @ `5d03ebf`. Android group `63f5a117-bf69-43a2-8ba0-9fc5b6479a0a` (runtime `c1907ba4…`); iOS group `3930c7e2-904c-4947-86f7-c086c220a148` (runtime `5359dcce…`). Fingerprints measured unchanged before publishing. **Published only after the migration was live and V1–V7 all passed** — the caption query selects `strain_updated_at`, so shipping it first would have returned nothing for every user.

**By-construction, not corrective.** `strain_updated_at` is the CYCLE's own `whoop_updated_at`; `source_updated_at` stays exactly as it was. On all current production data the two are **identical** — 116 WHOOP rows, zero divergence — so this migration changes no value anyone can see. It exists so the caption is right by definition rather than by WHOOP's current behaviour, which nothing enforces.

**Both view bodies were generated from live `pg_get_viewdef`**, not retyped from the migration files, so the diff is exactly nine added lines plus two trailing commas. Three things about the shape, each of which would have broken it:
- The new column is **last** in the resolved view's final select. `CREATE OR REPLACE VIEW` can only append; putting it beside `source_updated_at` where it reads more naturally would have shifted `sleep_source_record_id` from 33 to 34 and been rejected.
- The **Health Connect arm of the `UNION ALL` carries `null::timestamptz`**, as it already does for `source_updated_at`. Without it the two sides' column lists don't line up and the view fails to create.
- **`security_invoker` is stated explicitly** on both rather than assumed to survive the replace. These views have no RLS of their own; one silently losing the option is a cross-user read, not a bug.

### Verification — run against the live views after applying

| Check | Expected | Result |
|---|---|---|
| V1 column counts | 27 / 34 / 75 / 27 | **PASS** — dependents did not move |
| V2 column-list md5 excluding the new column | identical to the pre-apply baseline | **PASS** on all four |
| V2b new column position | 27 and 34 | **PASS** — appended, nothing shifted |
| V3 equals the cycle's timestamp | 116 rows, 0 mismatches, 0 nulls | **PASS** |
| V4a Health Connect frames | 367 rows, 0 non-null | **PASS** |
| V4b `security_invoker` | on, all four | **PASS** |
| V5 privileges | 12 rows, unchanged | **PASS** |
| V7 dependents still resolve | 483 / 483 | **PASS** |

**V6 (RLS, impersonated) — PASSED, and the migration is verified.** Pre-apply baseline, SQL editor, impersonating `a8435663…` at 16:16 BST: **70 rows, 1 user**, against an unimpersonated **479 rows, 5 users** — that contrast is what makes the check discriminate. Post-apply, same user, 16:27 BST: **70 rows, 1 user, identical**. `security_invoker` survived the replace in behaviour as well as in `reloptions`, which is the assertion that actually matters: these views have no policies of their own, so one losing the option would read as its owner and return every user's rows.

### The caption

`getWhoopScoresForDate` now selects `strain_updated_at`, and the panel renders `formatStrainCaption(scores.asOf)` — the same day-aware formatter written and tested back in PR 4, unchanged. It shows only when there **is** a strain value: a timestamp under a dash claims the dash is fresh. Absent for Health-Connect-only days (no cycle, so no calculation time) and for future days.

**Verified:** Own device pass on the dev build, reported by Robbie 2026-09-20: **passed on Pixel**. Today's strain read "as of HH:MM" and the time matched WHOOP; a past day read "as of <weekday> HH:MM"; future days and days with no strain showed no caption at all; the caption fitted at the largest system font. **iOS owed to testers.** Not yet exercised by a tester, so this stays `Fixed`, not `Verified`.

**Tests:** 27 in `whoopScores.test.ts` and 16 in `whoopScoresPanel.test.ts`. Five sabotage runs: reading `source_updated_at` again, rendering the caption without a strain value, the panel formatting the time itself instead of using the shared formatter, and `asOf` surviving a missing strain — each fails exactly one test.

**The fifth sabotage passed, and that was the useful one.** Swapping the column in the `.select()` string broke nothing, because the mock returns whatever fixture it is handed regardless of what was selected. In production that would have been **silent**: `row.strain_updated_at` undefined, `asOf` null, and the caption simply never appearing — no error, no failing test, just a feature that quietly does nothing. There is now an assertion on the select list itself, and the sabotage fails it.

### Corrections shipped with this

The claim that `source_updated_at` diverged from the cycle's timestamp in **2 of 172 rows (1.2%), by up to 17.6 hours** was **wrong and is withdrawn** from both records and from `WhoopScoresPanel.tsx`'s comment. It came from joining `whoop_cycles` on `id` alone — see the per-user-ids constraint above. The real figure is **116 rows, zero divergence**.

That figure was the entire empirical justification for this migration, and it did not survive contact with a correct query. The work went ahead anyway on the structural argument, which is weaker and was stated as such before starting.

---

## PR 5 — the WHOOP scores panel (no caption)

**Shipped in:** OTA, production channel, 2026-09-20, master @ `3fb3373`. Android group `7c0b4336-d65c-409d-a7b9-29f15188be27` (runtime `c1907ba4…`); iOS group `6f4454b1-5773-4270-b80f-f36fa7b974e8` (runtime `5359dcce…`). Fingerprints measured unchanged on both platforms before publishing.

- **The column is a fixed 82dp and renders on every page for a connected user**, including a future date where all three read "–". That is the layout contract: a column that sized itself to its content, or disappeared on days with no data, would slide the ring sideways as you swipe. A dash that holds its place beats a number that moves the page.
- **`CalorieRing` is untouched.** The ring keeps its fixed 200dp and the offset is the enclosing row's business — 200 + 12 gap + 82 = 294dp of the 328dp available inside the card at 360dp.
- **One "–" for every kind of unknown**, defined once in the component: a NULL value, a pending or unscorable score, an unrecognised state, no row, an undated cycle, a future date, a failed read. `getWhoopScoresForDate` collapses them all to null so the panel has a single case to render. The unit is dropped beside a dash, because "– %" reads as a broken number rather than an absent one.
- Recovery and sleep as integer percent, strain to 1dp. "WHOOP" as **text, no logo**. Theme tokens only. The numbers use the mono face, whose digits are tabular, so a value going from 8 to 88 cannot shift the column.
- **Gated on the store's connection state**, seeded from the local cache in PR 4, so the first frame is already right for a returning WHOOP user. Settings' local state is not consulted.
- **Per-page fetch, keyed on that page's own date.** The 3-page window means the neighbours fetch too, deliberately — swiping should not wait. An in-flight read is cancelled when the page changes, without which a slow read for yesterday can land after a fast one for today and write the wrong day's numbers.
- **Refresh joins PL-018's post-sync callback** rather than polling: the store gains `biometricRefreshToken`, bumped by the same `refetchIfSyncWroteData` refetch that re-reads workouts, and the panel's effect watches it.

**The strain caption is deliberately absent.** Decision (Robbie, 2026-09-20): ship the panel now, add `strain_updated_at` to the view in PR 6, then wire it. `formatStrainCaption` is already written and tested in `src/lib/whoopScores.ts` — it is correct and independent of which column feeds it, so it stays. A test asserts the panel does **not** render it, so it cannot drift back in before the column exists.

**Tests:** `src/components/__tests__/whoopScoresPanel.test.ts` (16). Source text rather than a render, for the same reason as `insightsStub.test.ts` — these components need an RN runtime this project's Vitest setup deliberately doesn't provide. The **values** behind the panel are covered for real in `whoopScores.test.ts` (25 tests); what is asserted here is the layout contract and the wiring. Eight sabotage runs, each discriminating: a content-sized column, the unit rendered beside a dash, a hard-coded colour, strain as an integer, the caption sneaking back in, the panel rendered unconditionally, no cancellation on page change, and the fetch ignoring the page date.

**Verified:** Own device pass, reported by Robbie 2026-09-20: **passed on Pixel**. Values matched the WHOOP app on three dates. Page-following held **including fast swiping** — the in-flight cancellation is the thing that makes that true, and it is the one behaviour here a slow network could have exposed and a careful swipe would not. A future page showed “–” with the ring fixed in place; **no jump on cold start, screen-recorded**; airplane mode kept the column; disconnecting in Settings hid the panel without a restart; switching to a non-WHOOP account showed **no flash** of the column. Fit held at `wm density 480` with the largest system font. **iOS not run — owed to the next tester session.** Not yet exercised by a tester, so this stays `Fixed`, not `Verified`.

**Original device list (Pixel + iOS), caption checks removed:** values match the WHOOP app on 3 dates; swiping follows the page's date, including across midnight; a future page shows "–" with the ring in place; a non-WHOOP account sees an unchanged, centred ring card; disconnecting in Settings hides the panel without a restart; no layout jump on cold start (screen-record it); sign out then in as a non-WHOOP account shows the centred ring with **no flash** of the column; an airplane-mode cold start keeps the column with "–". At default density, `wm density 480` and the largest font; iOS at 375pt and the largest Dynamic Type.

---

## PR 4 — WHOOP connection state and `getWhoopScoresForDate` (no UI)

Not a defect. A feature slice, recorded here because two decisions and one measured caveat came out of it that the next round needs.

**Shipped in:** OTA, production channel, 2026-09-20, master @ `3fb3373`. Android group `7c0b4336-d65c-409d-a7b9-29f15188be27` (runtime `c1907ba4…`); iOS group `6f4454b1-5773-4270-b80f-f36fa7b974e8` (runtime `5359dcce…`). Committed at `dcf43f0`; shipped together with PR 5, which is the first thing to render any of it.

### The cold-start layout decision

Today's WHOOP panel sits in an 82dp column beside the calorie ring, so "is WHOOP connected?" decides the **layout**, not just the content. The connection row cannot be read before the Supabase session is restored — `getSession()` awaits `initializePromise`, and supabase-js resolves the access token per request — so a cold start that waits for the network renders the centred, no-column layout and then jumps the ring sideways when the row lands. Every launch, for exactly the users the feature is for.

**Measured:** the `whoop_connections` round trip is **35 ms min / 42 ms median / 76 ms p90 / 89 ms max**, from the dev machine on a wired connection, 10 samples after 2 warm-ups. That is a **floor, not the answer** — it is not a Pixel on mobile data, and the query cannot start until the session is restored, so the real delay is `AsyncStorage read + auth init + one round trip`. The first two were not measured; doing so needs a build and a timed log on the device.

**Decision (Robbie, 2026-09-20):** seed the connection state from a local AsyncStorage cache, then reconcile. The stale-revoked case — a user who revoked on WHOOP's site since the last launch sees the panel with "–" for one round trip — is **accepted**.

Conditions, all built: the cache holds `{ userId, connected }` and nothing else (no tokens, no scores, no connection-row fields); a cache whose `userId` doesn't match the restored session counts as missing; `reset()` removes it; a failed reconcile **keeps** the cached state and reports once (PL-014), so only a *successful* read moves the layout; Settings' connect and disconnect write the store and the cache in one action.

`@react-native-async-storage/async-storage` was already a direct dependency at `2.2.0`, so `package.json` is unchanged and there was no fingerprint question to answer.

### The strain caption — decision, and a measured caveat

**Decision (Robbie, 2026-09-20): `asOf` is `source_updated_at` — WHOOP's own last calculation, not our pull time.** For a score, WHOOP's calculation time is the honest "as of"; when plated last pulled the row says nothing about the number's freshness. **This supersedes the earlier "strain shows last-synced time" default**, and `whoop_connections.last_sync_at` is not used for this at all. Everything in the code and tests that said `synced` has been renamed to match what the value is.

**Caveat, measured, and it is not what the column name suggests.** `biometric_periods` defines it as:

```sql
greatest(c.whoop_updated_at, r.whoop_updated_at, s.whoop_updated_at) as source_updated_at
```

— cycle, recovery **and** sleep, described in the migration as "freshness signal across all three joined sources". `biometric_periods_resolved` passes it through unchanged. So it is **"when this WHOOP frame last changed"**, not "when strain was calculated", and a sleep or recovery record updated later than the cycle carries the caption with it.

**Withdrawn measurement.** This record originally claimed 2 of 172 rows (1.2%), up to 17.6 hours. That was a join artifact — see the corrected figure in the `source_updated_at` constraint above. The real number is **116 rows, zero divergence**. The concern is structural, not observed: nothing enforces `GREATEST(c, r, s) = c`.

Separately: the Health Connect arm of the resolved view selects `null::timestamptz` for this column, so an HC-sourced period has no caption at all. That falls out correctly — `asOf` null means no caption — but it means the caption is a WHOOP-only affordance.

### Tests — and how they were written

**Connection: 15 tests, 8 sabotage runs. Scores: 25 tests, 8 sabotage runs.** 731 total green, `tsc` clean.

**These were written AFTER the implementation, not red-first.** Both suites. That is a departure from the rule this project runs on, and it is recorded plainly rather than implied away: the sabotage runs are the **only** evidence these tests discriminate, where a red-first run would have been independent evidence as well.

That mattered twice. Two of the connection tests were **vacuous** and a sabotage run caught it: the "different user" and "first frame" cases asserted the **end** state, after reconcile had already written the correct answer, so deleting the user-id check entirely still passed. Both now assert *during* the seed window with the fetch deliberately outstanding — the window Today's first frame actually renders in.

Sabotage coverage: ignoring the cache's user id; treating a failed fetch as not-connected; skipping the cache seed; `setWhoopConnected` not writing the cache; `reset()` not clearing it; no user-id re-check after the round trip; caching the whole connection row; `revoked` counting as connected. And on the scores side: accepting any score state; placing undated rows; dropping the future guard; the future guard in UTC; no ordering on a doubled date; no failure report; the caption comparing UTC days; the caption surviving a missing strain.

### `PENDING_SCORE` and `UNSCORABLE` are fixture-only

Both are handled — only a `SCORED` state yields a value, and an **unrecognised** state is treated as not-a-score too, so a state WHOOP invents later shows "–" rather than whatever sits in the value column.

**Neither has ever appeared in production.** All three state columns hold only `SCORED` and NULL (recovery 112/371, strain 116/367, sleep 112/371). So this behaviour has **fixture coverage only, and no device coverage is possible yet** — a device pass cannot exercise a state WHOOP has never sent us. If one shows up later, the panel is already correct; nobody will have watched it be correct.

---

## Design decision — a per-install analytics id for Sentry, scheduled after the WHOOP panel

Not a defect and not a PL- issue — a decision taken 2026-09-20, recorded so it isn't re-argued.

**The problem it solves.** Sentry currently cannot tell "1 user hit this 400 times" from "400 users hit it once". `instrument.ts` sets `sendDefaultPii: false` and `beforeSend` does `event.user = undefined` ("we never attach a user; hard-clear in case anything populates it"), and nothing in the app calls `Sentry.setUser`. So every issue's "users affected" is empty. That surfaced while scoping the PL-003 and PL-017 Sentry queries, both of which ask for distinct users and cannot get it.

**Decision: yes, but a random UUID, not a hash of the Supabase user id.** Hashing the user id is only *pseudonymisation* — we hold the id set, so the mapping is reversible by enumerating our own users against the hash. A UUID generated on first launch and stored locally is a genuinely weaker identifier: it carries no relationship to the account, it does not survive a reinstall, and it does exactly the one job needed, which is separating one noisy install from many quiet ones.

**It is still an online identifier under UK GDPR**, so it needs a line in the privacy policy (`https://platedapp.uk/plated-privacy.html`, linked from `AboutScreen.tsx:20`) before it ships. That is part of the work, not a follow-up to it.

**Scheduled after the WHOOP scores panel (PR 4 / PR 5).** It changes nothing retrospectively — a fresh id can only attribute events from the build that introduces it, so shipping it sooner does not improve the PL-003 or PL-017 counts currently owed. Those stay uncounted-by-user whichever order this lands in.

**Notes for whoever builds it:** it will need the `event.user = undefined` line in `beforeSend` narrowed rather than removed (the point of that line is that the *Supabase* user never reaches Sentry — that must stay true); `@react-native-async-storage/async-storage` is already a dependency and is the natural home for the id; and it must be generated, never derived from any account, device or install identifier.

---

### PL-028 — A product with no nutrition data in Open Food Facts is stored as all-zero, and a bundle freezes that forever

- **Severity:** Major
- **Reported as:** Robbie, 2026-09-20: "Chia Seeds (AKSOY, 10 g) was logged at 43 kcal / 2.2 P / 0.7 C / 2.9 F up to 2026-09-08. From 2026-09-14 it's 0 for every nutrient."
- **Status:** Logged — investigated read-only, no fix proposed yet, as asked.

**The reported timeline is the wrong way round, and that matters.** Pulling every row for barcode `5060731352170` (54 entries, 15 Jul → 20 Sep) shows the zeros are the **default state of this product, not a September regression**:

| Dates | Source | Values |
|---|---|---|
| 15 Jul | `barcode` (the original scan) | **all zero** |
| 16 Jul – 14 Aug | `bundle`, `copied` | **all zero** |
| 16 Aug – 7 Sep | `custom`, then `copied` | 42.70 / 2.19 / 0.69 / 2.86 |
| 1 Sep, 18 Sep | `custom` | 43.70 / 2.21 / 2.38 / 1.90 |
| 14 Sep | `bundle` | **all zero** |
| 15 – 20 Sep | `copied` | **all zero** |

So the **correct values were the exception** — they came from a custom food created by hand on 16 Aug, not from the product. The 14 Sep row didn't break anything; it restored the original state.

**Root cause — confirmed at every step, in order.**

1. **Open Food Facts has no nutrition data for this product.** Queried live: barcode `5060731352170` returns `status: 1` with `product_name: "Chia Seeds"`, `brands: "AKSOY"`, and **0 of 8 nutriment fields present** — not zeros, *absent keys*. Same for `20699321` ("Brown onions"), 0 of 8. This is PL-012's "check the OFF source first" answered: the app is not mis-parsing or mis-scaling anything, and there is no per-100g bug. OFF genuinely knows nothing.
2. **`parseProduct` coalesces the big four to 0.** `src/lib/openfoodfacts.ts:254` (`cal = cal ?? 0`) and `:313-316` (`(n["proteins_100g"] ?? 0)`, same for carbs and fat). The **small four are handled correctly** and stay `undefined` — which is why the Brown onions items carry NULL sat fat/salt/fibre/sugar beside a zeroed big four. The file says why, at `:301-305`: "OFF itself sometimes reports those as a genuine (if low-quality) 0, which the adapter must pass through faithfully."
   **That reasoning conflates two different facts.** "OFF reports 0" and "OFF has no key at all" both become `0`, and only the second is a lie. They are trivially distinguishable — this product has *zero* nutriment keys — without touching the NOT NULL columns or `FoodProduct`'s non-optional `cal_per100`.
3. **A bundle freezes the snapshot, correctly, and that is what made it permanent.** Two bundles contain this item: **"Ultimate Yogurt Breakfast"** (created 15 Jul) holds it **all-zero**, and **"New Breaky"** (created 29 Aug, after the user's manual correction) holds it at 42.7 / 2.19 / 0.69 / 2.86 with `salt` correctly **NULL**. Applying the old bundle on 14 Sep wrote the July snapshot back, and copy-forward propagated it for six days. **The apply path and the bundle design are both working as intended** — freezing values is the point of a bundle. The defect is entirely in what was frozen.

**Blast radius — smaller than the raw count, and two of the five "defects" aren't defects.**

Five bundle items across 4 bundles and 2 users have an all-zero big four, out of 46 items in 9 bundles. But:

| Item | Bundle | Verdict |
|---|---|---|
| Chia Seeds, 10 g | Ultimate Yogurt Breakfast | **Wrong** — chia is ~486 kcal/100 g |
| Brown onions, 150 g | Bolognese | **Wrong** — onions are ~40 kcal/100 g |
| Brown onions, 100 g | Salt pepper chips and chicken | **Wrong** |
| Salt, 6 g ×2 | Three bean chilli and avocado salsa | **Correct.** Table salt genuinely has 0 kcal / protein / carbs / fat, and these carry `salt = 5.895 g`, which is right for 6 g. A true zero, not a coerced one. |

So **3 real items, 2 users**. In `meal_entries`: **32 zeroed rows for the chia barcode across 2 users** (1 `barcode`, 10 `bundle`, 21 `copied`), spanning 15 Jul – 20 Sep. The second user has them via friend-copy. **Brown onions have produced no entries at all yet** — those two are **latent**, sitting in bundles waiting to be applied.

**Recoverability — different answers per item, and neither is a measurement.**

- **Chia Seeds: recoverable in principle.** The same barcode at the same 10 g serving has a known-good sibling — the "New Breaky" bundle item, plus ~20 correct `meal_entries` from 16 Aug – 7 Sep — all at 42.70 / 2.19 / 0.69 / 2.86. The 32 zeroed rows could be repaired from the user's own later correction.
- **Brown onions: not recoverable from anything we hold.** OFF has nothing, there is no custom food, and no non-zero row exists for that barcode anywhere in the database.
- **Both repairs would be inference, not measurement**, which is the same wall PL-002 and PL-012 hit: a stored 0 cannot be told apart from a real zero, and the two Salt rows above are precisely why a blanket "all-zero big four → repair" sweep would be wrong. Any repair has to be per-item and evidenced.

**Relationship to existing issues.** This is **PL-011** ("`meal_entries` big four can't represent unknown", Deferred) with a measured cost attached, and the mirror of **PL-002** (which fixed exactly this coercion for the small four in `saved_ingredients`). **PL-012** ("Tesco Gold Coffee library row saved all-zero — check the OFF source first") is almost certainly the same defect; that check is now done and the answer is "OFF had no data". Worth re-reading PL-012 against this.

### What was already fixed, and what was actually still open

**The all-zero barcode path was closed on 2026-08-15**, by `hasUsableNutrition` / `needsManualEntry` (`cb51fd5`, "manual nutrition entry for products with no usable OFF data"). `ProductScreen` has blocked Add for an all-zero OFF product since that date.

**All four affected products predate it**: Chia Seeds scanned 15 Jul, cucumber 23 Jul, Fibre & Protein Bar 12 Aug — three days before the gate — and Brown onions' bundle items 30 Aug and 2 Sep were built from an already-zeroed library row. **Not one `source: barcode` zeroed row was created after the gate shipped.**

**So PL-028 is mostly a DATA REPAIR issue, not an open ingestion path.** What was genuinely still open, and is now closed:

1. **The partial-key case.** `hasUsableNutrition` fires only when all four are zero, so OFF holding calories, protein and fat but no carbohydrates key stored `carbs = 0` and sailed through. `missingMacros()` closes it, keyed on key PRESENCE so a genuine zero (water, black coffee) still passes.
2. **Bundle apply.** A bundle freezes its items, so one built in July still writes those zeros today — that is exactly how the 14 Sep chia rows were created, a month after the gate. Now prompted via `hasNoNutritionData`.

### Cause A and cause B

**Cause A — OFF has no data and the app stores 0 anyway.** Chia Seeds (`5060731352170`) and Brown onions (`20699321`) return `status: 1` from OFF with **0 of 8 nutriment keys**. `parseProduct` coalesced the big four to 0.

**Cause B — data that was not yet available became a permanent fact.** cucumber (`5010251733911`) and the Bio&me bar (`5060853641220`) have **complete, 8-of-8 nutrition in OFF today**, yet were stored all-zero on 23 Jul and 12 Aug. OFF is crowd-sourced; products get completed over time. The app wrote `0` for something that was going to become known, and `?? 0` made that permanent.

Cause B is the more interesting half: it means the defect is not only "OFF is missing data" but "the app commits to an answer before the answer exists".

### Why a blanket repair sweep is wrong

Five bundle items have an all-zero big four. **Two of them are correct**: "Salt, 6 g" in "Three bean chilli and avocado salsa", carrying `salt = 5.895`. Table salt genuinely has 0 kcal, 0 protein, 0 carbs and 0 fat.

A rule of "all four zero → repair" would overwrite real measurements to fix fabricated ones — corrupting good data in the name of data integrity. This is the same wall PL-002 and PL-012 hit: **a stored 0 cannot be told from a real zero**, so every repair has to name its evidence per product. It is also why `hasNoNutritionData` asks "does this row assert anything positive at all?" rather than "are the big four zero?".

### The chia repair source, and how it was chosen

Two nutrient signatures exist among the non-zero chia rows — **and they belong to two different people**, which the first pass of this analysis missed:

| | kcal | P | C | F | sat fat | fibre | sugar | rows | account |
|---|---|---|---|---|---|---|---|---|---|
| **Sig 1** (16 Aug – 7 Sep) | 42.70 | 2.19 | 0.69 | 2.86 | 0.33 | 3.13 | 0.08 | 20 | `a8435663` — Robbie |
| Sig 2 (1 & 18 Sep, `custom`) | 43.70 | 2.21 | 2.38 | 1.90 | 0.33 | 2.12 | 0.08 | 2 | `f3b49ef9` — **another user** |

**Correction, measured after the repair was approved and before it was run.** This section originally described "21 non-zero pre-14-Sep chia rows" carrying two signatures, as though both were Robbie's. They are not. Sig 1 is entirely Robbie's — one `custom` row on 16 Aug, copied 19 times through to 7 Sep. **Sig 2 is entirely a third user's**, and one of its two rows falls after 14 Sep, so it was never inside the "21" at all.

This is the **same error class as the WHOOP `id` join** recorded above: rows grouped by nutrient value across the whole table with no `user_id` in the grouping, so two people's data pooled into one analysis. It did not change the outcome — Robbie's instruction was to use his own pre-14-Sep entries, and those are Sig 1 — but the record asserted something false about whose data was being weighed, and the Atwater argument below was being run against a stranger's hand-typed row. **Grouping by a value is a join on that value, and it needs `user_id` for exactly the same reason a join does.**

**Sig 1's macros are used; Sig 2's are not.** Sig 2 matches the packet's 437 kcal headline but fails every other check: by Atwater (EU convention, fibre at 2 kcal/g, carbs excluding fibre) its macros compute to **397 kcal against its own stated 437 — 9.2% out**, and its per-100 g profile (23.8 g net carbs, 19.0 g fat, 21.2 g fibre) does not resemble chia, which is roughly 7.7 / 30.7 / 34.4. It looks like a hand-typed custom food where total and net carbs were confused — and, being another user's row, it is **neither evidence for Robbie's repair nor Robbie's to correct**. It is left exactly as it is.

Sig 1's macros compute to **435 kcal**, which is **0.5% from the packet's 437** and 1.9% from its own stored 427 — so the label and the macros corroborate each other, and the stored 427 is most likely the scan reading energy low.

**Repair source, per 100 g — calories from the packet, macros from Sig 1:**

> **437 kcal · 21.9 g protein · 6.9 g carbs · 28.6 g fat · 3.3 g sat fat · 31.3 g fibre · 0.8 g sugar · salt NULL**

Scaled per row by `serving_g / 100`. The 42.70 bundle item in "New Breaky" is corrected to the same figures.

**Salt stays NULL.** It was never known, and writing 0 there would be the same defect this whole issue is about.

### The repair — applied to production 2026-09-20

Approved by Robbie after the dry run, then run as `BEGIN … COMMIT` with a body **byte-identical to the dry run he reviewed** — the only differences being the header comment and `commit` in place of `rollback`, asserted programmatically before it ran rather than by eye.

**It wrote to two accounts, both Robbie's:** `a8435663` (his main account) and `4dbf04ae` (his test account). Every matched row was attributed by `user_id` **before** the run, not assumed — including confirming that the third account holding chia rows belongs to a real, unrelated user and is written by nothing here.

| Account | Product | Rows |
|---|---|---|
| `a8435663` | Chia Seeds | 31 |
| `a8435663` | cucumber | 2 |
| `a8435663` | Fibre & Protein Bar | 1 |
| `4dbf04ae` | Chia Seeds | 1 |
| | **`meal_entries` total** | **35** |
| `a8435663` | `saved_ingredients` — chia, cucumber, bar | 3 |
| `a8435663` | `meal_composition_items` — both chia bundle items | 2 |

**Post-commit readback, run as an independent query against committed state** rather than from inside the repair transaction:

| Check | Result |
|---|---|
| Zeroed `meal_entries` left — chia / cucumber / bar | **0 / 0 / 0** |
| Zeroed library rows left — chia / cucumber / bar | **0 / 0 / 0** |
| Zeroed chia bundle items left | **0** |
| `salt IS NULL` on every repaired chia row | **32 of 32**, none written |
| `salt IS NULL` across all 54 chia rows in the table | **54**, none written |
| Salt bundle items unchanged | **2** — still 0 kcal, still carrying `salt = 5.895` |
| Brown onions bundle items unchanged | **2** — still all-zero |
| The third user's chia rows unchanged | **2** — still Sig 2 |
| Backup rows | **41** |

**Repaired values, per row.** Chia 10 g → 43.7 kcal · 2.19 P · 0.69 C · 2.86 F · 0.33 sat · **NULL salt** · 3.13 fibre · 0.08 sugar. cucumber 50 g → 26 · 1.07 · 4.24 · 0.19 · 0.06 · 0.02 · 1.0 · 2.785. Fibre & Protein Bar 40 g → 146 · 9.2 · 13.6 · 4.4 · 0.84 · 0.232 · 7.6 · 5.16.

**Brown onions is the one exception to "no zeroed rows left", by design.** Its 2 bundle items are still all-zero, because OFF has no data for barcode `20699321` and there is no honest source to repair from. Writing a guess there would be the original defect wearing a different hat. They are covered by the new apply-time prompt instead.

### The rollback path — `public.pl028_repair_backup`

| | |
|---|---|
| **Table** | **`maintenance.pl028_repair_backup`** — moved out of `public` the same day, see PL-031 |
| **Rows** | **40** — 35 `meal_entries`, 3 `saved_ingredients`, 2 `meal_composition_items`. Was 41 before a third user's row was deleted |
| **Taken** | **2026-09-20, 16:04 UTC**, before any UPDATE ran |
| **Holds** | every row the repair could match, with its **pre-repair** values |
| **Status** | **kept deliberately — do not drop.** It is the only record of the old values |
| **Reachable by** | `service_role` and `postgres` only. RLS on, no policies, no app-role grants, schema not exposed to PostgREST |

It was over-wide on purpose: it captured all **4** library rows carrying those barcodes, not only the 3 that were written, so the row the zero-guard excluded would be on record as having been in scope and deliberately skipped. That fourth row turned out to belong to a third user and has since been deleted — the excluded row is documented here instead, which is the right place for it.

**This backup table was itself a security defect while it sat in `public`** — created exposed, live for about 20 minutes, caught by the post-commit readback. **Logged and fixed as PL-031**, which carries the detail and the rule that came out of it.

**The third user's row was deleted, and the table moved out of `public`** — both on Robbie's instruction, 2026-09-20, straight after the readback.

One backup row belonged to a third user: their chia library row, captured by the deliberately wide `WHERE` and never modified. It carried no rollback value, because nothing about it changed, and it was not Robbie's data to hold. Deleted. **The backup is now 40 rows — 35 `meal_entries`, 3 `saved_ingredients`, 2 `meal_composition_items`, across Robbie's two accounts only** (`a8435663` 39, `4dbf04ae` 1).

The table now lives at **`maintenance.pl028_repair_backup`**, not in `public`. See PL-031 for why, and for the rule that came out of it.

### This repair changed historical totals — 2026-09-20

**Recorded so a future "why did my numbers change?" has an answer.**

Until today, 32 chia entries contributed **0 kcal** to their days. They now contribute **43.7 kcal** each. So on every affected day:

- **Calories rise by roughly +44 per chia serving logged**, and more where a day holds two.
- Protein **+2.19**, carbs **+0.69**, fat **+2.86**, fibre **+3.13** per serving. Fibre is the largest proportional change — a day that read 5 g of fibre may now read 8 g.
- Two cucumber days gain **+26 kcal** each; one day gains **+146 kcal** from the Fibre & Protein Bar.
- **Salt moves the other way on chia days:** from a fabricated `0` to *unknown*, so salt totals that silently counted those rows as zero now exclude them.

**Affected dates run 2026-07-15 → 2026-09-20 on `a8435663`, and 2026-09-19 on `4dbf04ae`.**

**Device-verified on Pixel, 2026-09-20 — PASSED.** Robbie checked three repaired days against the figures computed from the backup table, and the app agreed with all three:

| Day | Before the repair | After | Change |
|---|---|---|---|
| 2026-09-20 | 1181 | **1225** | +43.7 |
| 2026-09-19 | 2436 | **2480** | +43.7 |
| 2026-09-18 | 2452 | **2496** | +43.7 |

This is the check that matters most, because it is the one a user would notice: the repair had to show up in the app's own totals, not only in a `SELECT`.

**History, Insights, the WHOOP correlation and any CSV exported before 2026-09-20 will disagree with the same view after it.** The new figures are the correct ones: the old ones understated intake, because a missing value was being stored as zero. Any analysis, screenshot or export taken before this date carries the old, wrong totals.

**Shipped in:** OTA, production channel, 2026-09-20, master @ `1d3b777`. Android group `d6e660b7-dab5-4e32-807b-657ee803bca1` (runtime `c1907ba4…`); iOS group `2a083ed4-f785-4ca6-8d52-8227fa9ca6a0` (runtime `5359dcce…`). Fingerprints measured unchanged on both platforms before publishing, so this reached installed builds as an OTA. **The data repair is not in this update** — it was applied directly to production earlier the same day and was already live for every client.

### The `?? 0` sweep across every ingestion path

Asked for as part of the fix: find every `?? 0` / `|| 0` on calories, protein, carbs or fat, anywhere a value can reach the database.

**There is exactly one big-four coalescing site in the codebase, and it is the one being fixed.**

| Site | What it does | Verdict |
|---|---|---|
| `openfoodfacts.ts:254` (`cal = cal ?? 0`) and `:314-316` (protein/carbs/fat) | The OFF adapter | **The defect.** Fixed by `missingMacros()` labelling which keys were absent |
| `macros.ts:240` — `(product.sat_fat_per100 ?? 0) * f` and the same for salt/fibre/sugar in `computeServingTotals` | Small four, **display only** | **Real but Polish — logged as PL-030.** Its single caller is `ProductScreen.tsx:463`, assigned to `preview`. The actual insert at `:528-539` preserves NULL correctly (`draft.sat_fat_per100 != null ? … : null`), so the preview card shows "0.0 g sat fat" for a value that will be stored as unknown. Nothing wrong reaches the database; what the user sees before logging disagrees with what is logged |
| `social.ts:775` — `sum + (r.calories ?? 0)` | Friend's day total | **Not a defect.** `meal_entries.calories` is NOT NULL, so this can never coalesce anything real; it is defensive against the row type being nullable. The code comment already says so |
| `entries.ts:348` — `(satFat ?? 0) + e.sat_fat`, guarded by `if (e.sat_fat != null)` | `sumBucket` accumulator | **Correct, and deliberately so.** The guard is what makes a null row contribute nothing rather than turning the total into 0 |
| `useStore.ts:550`, `HistoryScreen.tsx:58`, `TodayScreen.tsx:792,1034`, `ProductScreen.tsx:147` | Display coalescing on the small four | **Correct.** Read-side, rendering an unknown as 0 in a ring or bar; none reaches a write |
| AI photo (`mealRecognition.ts`), label scan (`labelExtraction.ts`), recipe scan (`scanRecipe.ts`), custom foods, copy, bundles | — | **No big-four `?? 0` at all.** Checked explicitly; these paths never coalesce energy or the big three |

So the surface was narrower than feared: one real defect, one Polish issue, and no second ingestion path writing fabricated zeros.

### PL-030 — ProductScreen's preview shows 0 for a small-four value it will store as NULL

- **Severity:** Polish
- **Found:** during PL-028's `?? 0` sweep.
- **Root cause:** `computeServingTotals` (`macros.ts:230-243`) returns `ServingTotals`, whose fields are all plain non-nullable `number`, so it coalesces the small four with `?? 0`. Its only caller assigns the result to `preview` (`ProductScreen.tsx:463`). The insert path alongside it (`:528-539`) correctly preserves NULL.
- **Effect:** the card you read before tapping Add says "0.0 g sat fat"; the row that gets written says unknown. No bad data is stored — the disagreement is between the preview and the database.
- **Fix:** _not scheduled. Either make `ServingTotals`' small four `number | null` and render "–", or have the preview read the draft's raw values for those four. The first is the honest one and matches how `ProductScreen` already blanks a NULL cell._
- **Status:** Logged


---

## Part 4 Phase 2 — the Data tab: History | Trends

**Built, local checks green, NOT yet device-passed.** 839 tests across 49 files, `tsc` clean, fingerprints measured unchanged on both platforms (`c1907ba4…` / `5359dcce…`) — `react-native-svg` was already a dependency, so the charts add nothing native and this ships by OTA.

### The two questions owed before Phase 2

**1. Trends plots `eaten`, not `towardGoal` — confirmed, and pinned by a test that would fail if they were swapped.**

`towardGoal` folds unconfirmed plans into an unsettled day, because plans count toward daily goals (CLAUDE.md). That is right for a ring asking "am I on track today" and wrong for a line recording what happened: on `towardGoal`, today's point would jump the moment a plan was added and drop again at midnight when the day settled — **a line that moves without anyone eating anything.**

The test adds a 900 kcal pending plan to a 400 kcal day and asserts the point stays at 400. It also asserts `getDaySummary(...).towardGoal.calories === 1300` on the same fixture, so the fixture is proven to *be* discriminating rather than passing because the plan was too small to notice. "Incomplete" is computed on `eaten` too, with its own test: a pending row's NULL salt must not flag the day.

**2. The store's entries array covers the window.** Answered in the PL-028 PR: `fetchEntries` is unbounded, and `entriesCoverage.test.ts` fails if a bounded window shorter than 14 days is ever introduced. Nothing further needed.

### A spec finding: three of the eight nutrients can never be "incomplete"

`calories`, `protein`, `carbs` and `fat` are **NOT NULL DEFAULT 0** on `meal_entries` — `20260818140000_meal_entries_null_not_zero.sql` made the small four nullable and deliberately left the big four alone. The column cannot hold "unknown", so there is no such thing as a partially-known calories day.

**Which means the hollow "incomplete" point can never appear on any of the three DEFAULT charts.** Incomplete is a real state and worth having, but only sat fat, salt, fibre and sugar can reach it. `NULLABLE_NUTRIENTS` states this in one place and a test asserts the big four are excluded, so the notation and the schema cannot drift apart.

Worth noting where the two halves of today's work meet: **PL-028's repair made incomplete MORE common, not less.** Thirty-two chia rows moved from a fabricated `salt = 0` to a truthful NULL, so a salt chart over Robbie's last fortnight now shows hollow points where it previously showed confident ones. That is the repair working.

### What the charts refuse to say

Everything in `src/lib/trends.ts` is about not lying with a very confident-looking object:

| State | Drawn as | Because |
|---|---|---|
| Day with no eaten rows | **a gap** — no point, line breaks | A point at y=0 asserts "you ate nothing that day" |
| Nutrient unknown on *some* eaten rows | a point, **hollow**, captioned | The value is a real undercount |
| Nutrient unknown on *every* eaten row | **a gap**, flagged incomplete, captioned separately | The day WAS logged — "no salt data" is not "not logged" |
| Today | a point, **hollow** | The day has not finished |
| A genuine measured zero | **a solid point at 0** | Black coffee really is 0 g of fat |
| Goals not `loaded` | **no goal line at all** | PL-026 — a line across the chart at a number nobody chose |

The line **breaks at every gap** rather than interpolating across it. A straight run from Monday to Wednesday is a drawn claim about a Tuesday there is no data for.

### The sabotage runs — and the two that passed

Sixteen sabotages across the pure layer and the UI. Fourteen were caught immediately. **The two that were not are the useful part of this section.**

**S3 — the window stepped by milliseconds instead of calendar days, and every test passed.** Replacing `setDate()` with `now.getTime() - back * 86_400_000` breaks both UK transitions: the BST→GMT day is 25 hours, so a fixed step lands on the 25th twice and loses a day off the far end; the GMT→BST day is 23 hours, so it skips the 29th entirely. My fixtures were anchored at **local noon**, where an hour of drift cannot move the date — so the broken and correct implementations agreed all the way across the boundary.

**This is the same mistake, in the same session, as the CSV DST sabotage (S4).** Knowing the rule was not enough; the second fixture reproduced it. The replacement anchors were found by *measurement* rather than reasoning — running both implementations over every hour around both transitions and keeping the times where they disagree:

- `2026-10-25 23:30` → the millisecond version emits **2026-10-25 twice** and drops the oldest day.
- `2026-03-30 00:30` → it **skips 2026-03-29** entirely and reaches an extra day further back.

Both are within 30 minutes of local midnight, which is the only place a one-hour shift can change a calendar date. The missing-day case is the worse of the two: Trends would render the transition day as a **gap**, which is the chart's notation for "you logged nothing" — the app would quietly tell someone they had not eaten on the day the clocks changed.

**U2 — a source-text test cannot tell a broken line from an unbroken one.** The structural assertion checked that `segments.push`, `p.value == null` and `segments.map(` were all present. Removing the break left **all three in place**, so the test passed while the chart drew straight across an unlogged day — defeating the single most important property of the whole feature.

The fix was not a better regex. The path building was **extracted into `buildPathSegments` in the pure layer**, where it is tested against real input: a series with a gap must produce two segments, the first ending before the gap and the second starting after it, with no coordinate at the gap's x. The structural test now asserts only what it honestly can — that the chart **delegates** rather than growing a second copy — and a sabotage that inlines a broken copy fails it.

**The general lesson: a structural test proves a shape, never a behaviour.** When the property that matters is behavioural, the code has to move somewhere it can be executed.

### Shape

| File | What it is |
|---|---|
| `src/lib/trends.ts` | All the aggregation. Pure, no theme import — `theme/tokens.ts` pulls in React Navigation, which does not survive vitest's node environment, so a lib module that reaches for a colour takes the whole pure-logic layer's tests down with it |
| `src/lib/trendsPrefs.ts` | Selection and range, per user, in AsyncStorage. Same user-id guard as `whoopConnectionCache`; every read re-validated against the current nutrient list, so a preference from an older build cannot name a chart that no longer exists. Cleared by the store's `reset()` |
| `src/components/TrendChart.tsx` | One small multiple. Own y-scale anchored at zero, colours from the per-macro language |
| `src/components/TrendsPanel.tsx` | Picker, 7/14 toggle, the charts. Computes nothing |
| `src/components/SegmentedControl.tsx` | History \| Trends. In-screen state, no nested navigator |
| `src/screens/DataScreen.tsx` | The tab. **Opens on History, every time** |
| `src/screens/HistoryScreen.tsx` | Unchanged except an `embedded` prop that skips its own safe-area inset and its own heading, both of which `DataScreen` already draws |

**The route was renamed, not just the label.** `BottomTabParamList.History` → `Data`, which drags `TAB_ICONS` and the navigator along by `tsc`, and `tabStructure.test.ts` now asserts the name is gone from all three files rather than merely changed in one.

**The segment is deliberately not remembered.** History is what people come to this tab for, and a tab that opens somewhere different depending on what you did last week is a tab you have to read before you can use. The nutrient selection inside Trends **is** remembered — that is a preference about content, not about where you are.

### Device pass — PASSED on Pixel, 2026-09-20

Data opens on History and History is unchanged; Trends matches Today; gaps and the "N not logged" caption are right; today's point is hollow and reads "so far"; the 7/14 toggle works; the selection persists across a relaunch and does **not** carry across accounts. **iOS owed to testers.**

Six pieces of polish came out of it, below. They ship in the same PR.

### Polish from the device pass

**1. Points were clipped at the edges.** Today's hollow point sat half outside the canvas. The padding had been sized for the **line**, but a hollow dot is wider than its centre by its radius plus half its stroke — and it is always the *last* point in the window, which is always the one at the edge. Padding is now derived from `POINT_EXTENT = POINT_RADIUS + POINT_STROKE / 2` on all four sides, with the top reserved so a value at the very top of the scale is not clipped either.

**2. The charts had no axes.** Now: x-axis dates — a short weekday for every day at 7, and d/M at 14, thinned to the first, the last and roughly every third, never two adjacent. y-axis — the top of the scale and `0`, with a baseline so zero is a *place* rather than a label floating beside nothing. The goal line reads **"goal 2,800"**, left-aligned, because today's point sits at the right edge and a label there would land on top of it.

**3. The y-axis starts at zero, for every nutrient, pinned by a test.** A min-based scale is the classic chart lie: 2,400 and 2,450 kcal become a dramatic climb because the axis starts at 2,395. Two tests hold it — one asserts `y(0) === plotBottom`, the other that two values 2% apart render less than 5% apart. The sabotage Robbie asked for (a `top * 0.98` floor) fails both.

**4. The leg into today is dashed.** A solid line dropping into a low point reads as a collapse rather than as a day that is only half over. `buildPathSegments` now splits that final leg into its own segment carrying `dashed: true`; the two share a coordinate, so the line still reads as continuous.

**5. The headline number says which day it is** — "today so far" when the latest point is today, otherwise the date. Without it the number reads as a total for the whole window, which it is not.

**6. "Pick up to 3" is now a standing hint** beside the chips with a live `n/3` count, and tapping a fourth chip answers with a brief message that clears itself after 2.6s. Previously it did nothing at all, which reads as a broken button rather than as a rule.

### The polish sabotage run — and the third source-text blind spot

Eleven sabotages, **all caught**, but one of them only after the test was rewritten — and it is the same blind spot for the third time in this feature.

**P10 first attempt: a 4th tap silently does nothing again.** The sabotage was `void 0 && setCapMessage(...)`, which leaves the call present but dead. The structural assertion matched `/setCapMessage\(/` and passed. **A regex cannot tell a live call from a dead one**, exactly as it could not tell a broken line from an unbroken one (U2) or a zero-anchored scale from a min-based one.

Fixed the same way, and the pattern is now explicit: **when the property that matters is behavioural, move it somewhere it can be executed.** The wording and the choice between the two refusals became `capMessageFor` in `lib/trends.ts`, tested against `toggleNutrient` **over every combination** of up to three nutrients — a message exists precisely when the toggle refuses, so the rule and the explanation of the rule cannot drift. What the structural test now asserts is only what it honestly can: that the panel holds no wording of its own. That the call is wired at all is a device check, and it passed.

**Three source-text tests in this feature have now failed to catch the thing they were written for.** The rule that came out of it is in the test files: a structural test proves a SHAPE — that a module delegates, that it holds no second copy, that a string is not inlined. It cannot prove a BEHAVIOUR. Anything behavioural belongs in the pure layer.

### Device pass — the re-check after polish

| Check | What to look for |
|---|---|
| No clipping | Today's hollow point is **whole**, not sliced by the right edge. Same for the first point on the left, and for the highest value against the top |
| x-axis | 7 days: a weekday under every point. 14 days: first, last and every third, **never overlapping** at 360dp and the largest font |
| y-axis | The top of the scale and `0`, with the baseline drawn |
| Goal line | Reads **"goal 2,800"**, not a bare number, and does not collide with today's point |
| Dashed today | The final leg into today is dashed; earlier legs are solid |
| Headline | Says **"today so far"** when today has a value, otherwise the date |
| The cap | "Pick up to 3" and `n/3` show before any tap; a 4th chip gives a brief message that clears itself |
| Still true | Data opens on History; Trends matches Today (2026-09-20 = **1,225 kcal**); gaps are gaps; **Salt** shows the PL-028 hollows; selection doesn't cross accounts |
| Insights unchanged | Still the AI-only stub |

---

### PL-031 — The PL-028 repair backup was created in `public`, so it was a published API endpoint

- **Severity:** Major
- **Found:** 2026-09-20, by the PL-028 post-commit readback. Not by a test, and not by review — the readback happened to print `saved_ingredients: 4` where the repair wrote 3, which prompted a look at what else the backup held and who could see it.
- **Live for:** roughly 20 minutes, from **16:04 UTC** (the backup insert) to the readback and fix.

**What happened.** The repair's rollback table was created as `public.pl028_repair_backup`, because that is where you create a table when you aren't thinking about it. On a Supabase project that is not a neutral choice:

1. **PostgREST serves the `public` schema**, so the table was addressable at `/rest/v1/pl028_repair_backup` the moment it existed.
2. **Supabase's default privileges grant `anon` and `authenticated` full DML on new tables in `public`** — measured on this table as `SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER` for both roles.
3. **`create table` does not enable RLS.** Nothing was denying anything.

The anon key is shipped inside the app, so this was not a theoretical exposure.

**What it contained.** 41 rows of pre-repair nutrition data with `user_id` on every row, spanning **three accounts** — Robbie's two and one unrelated user's. Readable by anyone with the anon key, and **truncatable** by them: the rollback path for a data repair was deletable by an anonymous client while the repair was being verified.

**Why the usual defences did not catch it.** Every `meal_entries` and `saved_ingredients` table in this project has RLS and policies, added by a migration that was reviewed. This table was created by an ad-hoc repair script that nobody reviewed as a migration, so it inherited the project's defaults instead of the project's conventions. **The defaults are the unsafe case.**

**The fix, in two steps.** First, immediately: `revoke all … from anon, authenticated` and `enable row level security` with no policies. Then, on Robbie's instruction, the stronger form — the table was moved out of `public` altogether into a new **`maintenance`** schema, which is not in the project's PostgREST-exposed schema list.

**Verified end-to-end afterwards, as an anonymous client rather than from the database side:**

| Probe with the shipped anon key | Result |
|---|---|
| `GET /rest/v1/meal_entries` — control, proves the key and request work | **HTTP 200**, `[]` (reachable, RLS denies rows) |
| `GET /rest/v1/pl028_repair_backup` | **HTTP 404**, `PGRST205` — not in the schema cache |
| Same, with `Accept-Profile: maintenance` | **HTTP 406**, `PGRST106` — "Invalid schema" |

The second result is the important one: the table is not merely *denied*, it is **not addressable**. PostgREST refuses to serve the schema at all, so no grant mistake on the table can ever re-expose it. Database-side state after the move: schema `maintenance`, RLS on, 0 policies, table grants `postgres` and `service_role` only, **no `USAGE` on the schema for `anon` or `authenticated`**, 40 rows intact.

**The rule, now in `supabase/repairs/README.md`:** repair artefacts never go in `public`. Not "go in `public` with RLS" — **not in `public`**. A table there is exposed from the moment it exists, and the window between `create table` and remembering to lock it down is exactly the window this defect lived in.

- **Status:** Fixed

### PL-032 — Two identical Salt items in the same bundle

- **Severity:** Minor
- **Found:** 2026-09-20, while listing the bundle items PL-028's repair deliberately left alone.
- **What:** the bundle "Three bean chilli and avocado salsa" (14 items) holds **two Salt items that are identical in every field** — both 6 g, both 0 kcal / 0 P / 0 C / 0 F, both carrying `salt = 5.895`.
- **Why it might not be a defect:** a recipe can legitimately salt at two stages, and both rows are correct data — table salt really is 0 kcal. This is the same "a stored value cannot be told from an intentional one" wall as PL-002, one level up.
- **Why it might be:** they agree to the fourth decimal place on a field a human would have to re-enter by hand. A duplicate insert is the more likely explanation, and the bundle's salt total is double-counted if so.
- **Effect if real:** the bundle contributes 11.79 g of salt instead of 5.895 g every time it is applied — roughly double the daily reference intake from one meal, which would quietly distort both the salt ring and any WHOOP correlation against it.
- **Fix:** _not scheduled, and deliberately not touched. Read-only observation; changing a user's recipe on a guess is exactly the mistake PL-028's "no blanket sweep" rule exists to prevent. Worth asking Robbie whether that recipe salts twice before anything is done._
- **Status:** Logged

---

## iOS coverage owed

Everything shipped in the 2026-09-20 goals and CSV rounds was device-passed on **Pixel only**. Recorded here rather than buried in five rectification entries, so the next iOS pass or tester session has one list:

| Issue | What to exercise on iOS |
|---|---|
| PL-017 | An account with no goals row shows the defaults and files no Sentry event |
| PL-023 | Airplane mode at cold start → the error state with Save disabled; online + Retry → real targets; editing one target changes only that column |
| PL-024 | Clearing a target disables Save with an inline error; an explicit 0 saves as 0 |
| PL-007 | A late-evening meal exports under its own local date; the filename matches |
| PL-008 | A row with a known NULL small four exports as empty cells, a real 0 as 0.0 |

All five are -only and shipped by OTA with identical fingerprints on both platforms, so iOS is running the same JS — but the keyboard, the share sheet and Dynamic Type are not the same, and none of those is exercised by the Android pass.

---

## Owed to testers — the two PL-028 scan checks

Robbie's own device pass on 2026-09-20 could not cover these: **he has none of the products to hand**, and both need a real barcode scanned through the camera rather than a library row tapped. Handed to testers with the exact barcodes, so nobody has to go hunting for a product with broken data.

**A third check is owed too, for a reason worth recording: the fix deleted its own test fixture.** The bundle prompt was to be exercised by applying a bundle containing the Brown onions item — but the two bundles holding Brown onions belong to a **different user**, and after the repair Robbie owns **no** bundle item and **no** library row with zero nutrition. The chia bundle item was the last one, and repairing it removed the only material on his account that could trigger the prompt. The UI cannot produce a replacement either: scanning `20699321` now hits the manual-entry gate, which is precisely the fix working.

Writing a zero-nutrition fixture into production purely to watch a prompt appear was **considered and rejected by Robbie** — a decision worth keeping, because the alternative is fabricating exactly the kind of row this whole issue exists to eliminate. The prompt is covered by unit tests and handed to the tester pass instead.

**Barcodes verified against the live Open Food Facts API on 2026-09-20** — OFF is crowd-sourced, so if someone completes one of these entries the check stops reproducing. Re-verify before a session; the note under each says what to look for.

### Check A — a product OFF knows nothing about

| | |
|---|---|
| **Barcode** | `20699321` — *Brown onions*, or `5060731352170` — *Chia Seeds (AKSOY)* |
| **OFF state** | `status: 1` (product exists, has a name) with **0 of 4 big-four keys** |
| **Scan it and expect** | the manual-entry gate: **all four** of calories, protein, carbs and fat blank, Add blocked until they are filled |
| **Must NOT happen** | four pre-filled zeros, or an Add that succeeds and writes a 0-kcal row |

Either barcode works. Brown onions is the better one — chia now has a repaired library row, so a tester who has previously logged it may reach it from their library rather than the API, and the library row is no longer all-zero.

### Check B — a product OFF describes only partly

| | |
|---|---|
| **Barcode** | `6111203006646` — *la hollandaise*, a Moroccan cheese |
| **OFF state** | **7 of 8 keys present, `carbohydrates` the only absent one**: 270 kcal, 9 protein, 21 fat, 12 sat fat, 1.5 salt, 0 fibre, 6 sugar |
| **Scan it and expect** | **only the Carbs cell blank.** Calories, protein and fat pre-filled with 270 / 9 / 21. Add blocked until carbs is filled |
| **Must NOT happen** | `carbs = 0` pre-filled alongside three real values — that is the exact defect PL-028 fixed, and it is invisible because the other three look right |

This is the one that matters. Check A was already closed on 2026-08-15 by `hasUsableNutrition`; **Check B is the case that was still open**, and a partial product is rare enough in the wild that it will not turn up by accident — **0 of 400 UK products sampled from OFF had a partial big four.** This barcode was found by scanning four OFF categories specifically to produce a tester fixture.

**A second, blunter variant if one is wanted:** `20242008` (Lidl, unnamed) has **calories only** — 46 kcal, and all seven other keys absent. Protein, carbs and fat should all be blank while calories shows 46. It is useful because the row still asserts something positive, so it must reach manual entry via `missing_macros` rather than via the all-zero test.

### Check C — applying a bundle whose item has no nutrition data

| | |
|---|---|
| **Needs** | a bundle containing an item with **no nutrition at all** — every one of the eight nutrients 0 or NULL |
| **Who has one** | not Robbie. Two such bundles exist on another account, both holding a Brown onions item (150 g and 100 g) |
| **Apply it and expect** | a prompt naming the item and saying it has no nutrition data, **before** anything is written |
| **Must NOT happen** | the bundle applying silently and adding a 0-kcal row to the day |

A tester who built a bundle before 2026-08-15 from a product OFF knew nothing about is the likely person to hit this. If no tester has one, it stays covered by `hasNoNutritionData`'s unit tests and this check stays owed rather than being manufactured.


---

## Notes for the next tester

_Not defects — changes a tester needs to know about before they start, so a moved control isn't reported as a missing one._

- **Friends now lives in Settings**, not the tab bar — Settings → Friends (directly after Sharing). The **Settings tab badge shows pending requests**, and the same count shows on the Friends row, so the two cannot disagree.

---

## Deliberately not changed

_Anything left as it is on purpose, and why. Saves re-litigating it next round._

| ID | Feedback | Why not |
|---|---|---|
| PL-019 | Orphan open cycle | Fixing the sync to reconcile deletions is a larger piece of work against a WHOOP behaviour we've seen once. The date-keyed read plus the "never use `is_current`" constraint removes the blast radius for both the Today panel and the readiness query. |
| PL-022 | Stale schema comment | Comment-only drift in an applied migration. The code comment already states the real behaviour. |
| PL-025 | Name-based variable resolution in the structural test | Needs a scope-aware resolver, which is a rewrite of that helper rather than a patch. Renaming the colliding local restores the guard today, and the failure mode is now written down. |
| PL-026 | Adherence % against unloaded goals | Display-only and writes nothing, so it is not in PL-023's class. Worth doing with the Insights work, where the `loaded`-only constraint has to be enforced anyway. |
| PL-027 | Today's ring on default targets in `error` | Same reasoning as PL-026, and the same fix shape. Both are a single pass over the consumers of `goals`, which is worth doing once rather than twice. |

---

## What went well

- The wake-anchored day mapping (`biometric_periods_resolved.local_date`) held up on live data: 0 duplicate dates across all three users, against 1/1/2 duplicated dates under the older onset-anchored mapping in `biometric_periods`.
- RLS on the WHOOP/biometric chain measured clean: all three views are `security_invoker=on`, every underlying table has an `auth.uid() = user_id` SELECT policy, and an impersonated read returned only the signed-in user's rows (69 rows, 1 user). The sabotage run (no role switch) returned 479 rows across 5 users, so the check discriminates.
- Both planned features are JS-only: the Android fingerprint was byte-identical (`735a68d1…`) with a full navigation prototype applied.

## Raw feedback

_No tester in this round — the issues above come from a source investigation and read-only production queries._

---

**Remember:** update the session table and the open/closed issue tables in `testing/README.md` after filing this.
