# Test session — self (dev pass) — 2026-09-26

**Build / version:** No device run. Found in the read-only receipt-scanner investigation against master @ `177ed0a` (`docs/2026-09-25-receipt-scanner-findings.md`, candidate C), then measured on the live database by Robbie.
**Device & OS:** none
**Format:** internal dev pass
**Goal of this round:** Stop the meal list silently truncating once an account passes the API's row cap.

Everything here was found by the developer, not a tester. Nothing in this doc can reach `Verified` until a tester exercises the flow after the fix ships.

**Numbering.** PL-050 was taken as the next free number: the highest in the README tables and the newest session doc (`2026-09-25-internal.md`) was PL-049. PL-051 to PL-055 were added the same day, from PL-042's step-1 review of the staple seed. PL-050 was then the highest in the README tables and in every session doc.

---

## Issues raised

| ID | Severity | What happened | Status |
|---|---|---|---|
| PL-050 | Major | `fetchEntries` selects every `meal_entries` row in one unpaged request, and PostgREST silently caps a response at `max_rows` | In progress |
| PL-051 | Minor | No cooking-apple staple: a Bramley line has no staple of its own once `bramley apple` leaves `apple` | Logged |
| PL-052 | Major | `passata` staple is CoFID's homemade tomato sauce; CoFID has no passata row, and the importer can't delete a staple | Logged |
| PL-053 | Minor | `pepper` is an alias of both `bell-pepper` and `black-pepper`, and the staple lookup has no ORDER BY | Logged |
| PL-054 | Minor | `lemon` staple is the whole fruit with peel; the right row depends on how the recipe uses it | Logged |
| PL-055 | Major | Produce count weights in `unit_grams` are unsourced estimates, live in `estimateGrams`; a medium lime outweighs a medium lemon | Logged |

---

## Rectification log

### PL-050 — `fetchEntries` truncates silently at the API's row cap

- **Severity:** Major.
  - Nothing errors. Past the cap, the oldest-logged rows just stop reaching the store.
  - Today/History show those days as empty, the 30-day CSV can come out short, and a long-pending plan can miss the confirmation banner.
  - It's the wrong-numbers class, and invisible to the user.
- **Reported as:** Found in code review. The receipt-scanner investigation (`docs/2026-09-25-receipt-scanner-findings.md`, candidate C) noted `fetchEntries` has no range while `supabase/config.toml` sets `max_rows = 1000`.
- **MEASURED 2026-09-26:**
  - User A has 787 rows, at 8.4/day since 2026-06-24.
  - At that rate A crosses 1000 around 2026-10-21, if hosted Max rows = 1000. That setting wasn't confirmed at the time of measuring.
  - **Stopgap, same day:** hosted Max rows raised from 1000 to 2000 in the dashboard (no deploy). User A now crosses around mid-February 2027.
- **Root cause:** Confirmed from the code.
  - `fetchEntries` ran `select("*").eq("user_id", …).order("logged_at", desc)` with no `.range()` ([useStore.ts:638-649](../src/store/useStore.ts) at `177ed0a`).
  - PostgREST answers that with at most `max_rows` rows and no error, so the store held the newest `max_rows` rows as if they were all of them.
  - The order also had no unique tiebreaker. A bundle apply inserts several rows with one `logged_at`, so any paging on that order alone would read tied rows twice or not at all across a page boundary.
- **Fix:**
  - Page with `.range()` until a short page returns.
  - Order by `logged_at desc, id desc`, so every row has one fixed position and pages can't overlap or skip.
  - Page size is `ENTRIES_PAGE_SIZE`, below `max_rows` and pinned there by a test that reads `supabase/config.toml`.
  - A failed page throws inside the pager. `fetchEntries` reports it once and keeps the previous list: a partial list is never installed as if complete (the PL-003 rule).
  - Local `max_rows` set to 2000 to match hosted.
- **Where:** master @ `be8ed92` (red tests + filing) + `eec75b9` (fix). Code: `src/lib/paging.ts` (`fetchAllPages`), `src/store/useStore.ts` (`ENTRIES_PAGE_SIZE = 500`, `fetchEntries`), `supabase/config.toml` (`max_rows = 2000`).
- **Local checks (MEASURED 2026-09-26):**
  - **Red at `be8ed92`**, against the unpaged select and a fake capped at 1000 rows:
    - 1000 of 2,350 rows held;
    - 200 tied rows missing;
    - the failed-page case installed 1000 rows as if complete;
    - page-size tests red because the constant didn't exist.
  - **Green after the fix:** the new tests pass 11/11. The full suite passes **1033/1033 across 54 files** (was 1022/52). `tsc --noEmit` reports 0 errors. `entriesCoverage.test.ts` is unchanged and green.
  - **Sabotage 1, `.order("id")` removed:** the shared-timestamp test goes red with `{ duplicates: 4, missing: 4 }`. The 2,350 test goes red at 2,344 unique, and the exact-multiple test goes red on order. Restored, byte-identical, green.
  - **Sabotage 2, pager returns after the first page:** the 2,350 test goes red at 500. The shared-timestamp test goes red with 700 missing. The failed-page test also goes red: a 500-row partial list was installed over the previous one. Restored, byte-identical, green.
  - **Runtime versions** via `npx expo-updates runtimeversion:resolve`: Android `c1907ba4…`, iOS `5359dcce…`. Unchanged, so OTA-eligible.
- **Device checklist (owed, on the Pixel dev build before publishing):**
  - [ ] Today, History and Trends show the same days as before, with a pull-to-refresh on each.
  - [ ] Log a meal, then background and foreground the app: it's still there once and only once.
  - [ ] Settings → Export CSV: the row count matches `select count(*) from meal_entries where user_id = '<A>' and date >= current_date - 29;`.
  - [ ] Airplane mode, then refresh Today: yesterday's list stays on screen and doesn't empty.
  - [ ] **First-load failure:** cold-start the app in airplane mode, so the store has no previous list to keep. Record what Today shows.
    - If it's indistinguishable from "nothing logged", file it as a candidate, not a blocker.
    - Why it matters: a failed read keeps the previous list, which is recoverable. But on the first load the previous list is empty, so the day looks blank and no error shows.
- **Sweep of the other per-user selects (not fixed here; candidates, unnumbered):**

  | Read | Where | Pattern | Rows (what's known) | Risk |
  |---|---|---|---|---|
  | `biometric_workouts` | `useStore.ts:691-698` `fetchWorkouts` | Unbounded, **no ORDER BY at all** | Not measured. About one row per workout per source; Health Connect can backfill history (`READ_HEALTH_DATA_HISTORY`) | **Highest.** Past the cap it drops an arbitrary set, not the oldest |
  | `saved_ingredients_scored` | `useStore.ts:896-907` `fetchSavedIngredients` | Unbounded; ordered by `decay_score` only, which ties | 378 rows across all users, as predicted in `verify/20260919120000_verify.sql` (not measured here) | Low. Grows per distinct food. Needs an `id` tiebreaker if it's ever paged |
  | `meal_compositions` + embedded items | `compositions.ts:64-69` `getCompositions` | Unbounded; ordered by `last_used_at`, `use_count` (ties) | Not measured; tens per user at most | Low. PREDICTED: max_rows limits top-level rows, not an embedded list |
  | `follows` (state, friends, requests) | `social.ts:202-212`, `:400-419`, `:449-458` | Unbounded per user | Not measured; small | Low |
  | Friend range | `social.ts:521-529` `getEntriesForUserRange` | Bounded by a date range | — | Only if a caller asks for a very long range |
  | Friend day, today's calories, WHOOP day panel | `social.ts:481-492`, `:763-771`, `whoopScores.ts:96-106` | One day | — | None |
  | CSV export | `SettingsScreen.tsx:571-585` | Reads the store (`getAllEntries`), no query of its own | — | Fixed by this change |

  Single-row reads (`maybeSingle`) and `.limit()` searches (profiles, custom food by barcode, core ingredients, user search limited to 20) aren't affected.
- **Shipped in:** —
- **Verified:** No.
- **Status:** In progress

### PL-051 — No cooking-apple staple

- **Severity:** Minor. A feature gap, not wrong numbers.
- **Reported as:** Found in PL-042's step-1 review.
- **Root cause:** PL-042 drops `bramley apple` from `apple`'s aliases, because `apple` becomes CoFID's eating apple (14-319). The Bramley case is real, but it isn't an eating apple. Once the alias is gone, a recipe line "2 Bramley apples" finds no staple and falls through to OFF.
- **Fix:** Candidate, not started. Add a separate cooking-apple staple on 14-362, "Apples, cooking, raw, flesh only, peeled", with aliases such as `bramley apple` and `cooking apple`, and values from the CoFID file.
  - Not added in PL-042's pass: adding a staple widens the set, and that's its own decision.
- **Where:** —
- **Shipped in:** —
- **Verified:** No.
- **Status:** Logged

### PL-052 — `passata` is a homemade tomato sauce, and a staple can't be deleted

- **Severity:** Major. Wrong numbers, live in the recipe scanner.
- **Reported as:** Found in PL-042's step-1 review.
- **Root cause:**
  - `scripts/seedStaples.ts:264` pins `passata` (aliases `sieved tomatoes`, `tomato sauce`) to 17-834, "Sauce, tomato based, homemade". That row is 70 kcal and 4.5 g fat per 100 g: a cooked sauce with added fat, not sieved tomatoes.
  - `cofid.csv` has no passata row, so any code chosen here is wrong. The honest state is no staple, which lets the line fall through to OFF, the same reasoning as chicken in 1428e57.
  - Removing the entry from `SEED_STAPLES` isn't enough. The importer only upserts: a slug that leaves the list keeps its live row, and a staple hit short-circuits the OFF tier.
- **Fix:** Candidate, not started.
  - Give the importer a delete path for slugs no longer in `SEED_STAPLES`: either a reconcile step or an explicit `--delete slug`, dry-run first.
  - Then remove `passata`.
  - Do it together with the chicken cleanup: if PL-042's drift check (section C) finds `chicken-breast`/`chicken-thigh` still live, 1428e57 never took effect in production, and both need the same delete path.
- **Where:** —
- **Shipped in:** —
- **Verified:** No.
- **Status:** Logged

### PL-053 — `pepper` resolves to either pepper staple

- **Severity:** Minor. PREDICTED, not reproduced: a recipe line reading just "pepper" can resolve to the wrong food.
- **Reported as:** Found in PL-042's step-1 review.
- **Root cause:**
  - `bell-pepper` (`seedStaples.ts:135`) and `black-pepper` (`:155`) both carry the alias `pepper`.
  - `lookupStapleFromDb` ([ingredients.ts:281-300](../src/lib/ingredients.ts)) runs `.or(slug / aliases / display_name).limit(1).maybeSingle()` with no ORDER BY, so which of the two comes back is whatever order Postgres returns. An upsert rewrites both rows, so that order can change after any importer run.
  - Separately, `bell-pepper` is CoFID's red pepper (13-524) but carries the alias `green pepper`. Green is its own row (13-318, 15 kcal against red's 21).
- **Fix:** Candidate, not started. Take `pepper` off at least one of the two staples, and move `green pepper` off the red row (drop it, or give it its own staple). Whether the lookup should also get a deterministic order is a resolver change, which belongs to PL-035.
- **Where:** —
- **Shipped in:** —
- **Verified:** No.
- **Status:** Logged

### PL-054 — `lemon` is the whole fruit, peel included

- **Severity:** Minor.
- **Reported as:** Found in PL-042's step-1 review.
- **Root cause:** `lemon` (`seedStaples.ts:139-140`) is 14-128, "Lemons, whole, without pips" (19 kcal), which includes the peel. The file also has 14-129, "Lemons, peeled, flesh only" (14 kcal). A recipe lemon is zested, juiced or used whole, so neither row is right for every case. This is genuinely ambiguous, unlike PL-042's rule-based remaps.
- **Fix:** Undecided. Options: keep 14-128, move to 14-129, or split by use (zest / juice / flesh). Lemon juice has its own CoFID row, 14-277.
- **Where:** —
- **Shipped in:** —
- **Verified:** No.
- **Status:** Logged

### PL-055 — Produce count weights are unsourced estimates

- **Severity:** Major. They scale every counted recipe-scanner line that resolves to one of these staples.
- **Reported as:** Found in PL-042's step-1 review.
- **Root cause:**
  - These `unit_grams` weights have no stated source. The file's own header calls every seeded value "ESTIMATES, not facts" and lists produce counts under "REVIEW FOCUS":

    | slug | `unit_grams` | line |
    |---|---|---|
    | onion | small 90, medium 130, large 200 | `seedStaples.ts:127-128` |
    | tomato | small 90, medium 120, large 180, cherry 17 | `:129-130` |
    | carrot | small 50, medium 61, large 72 | `:131-132` |
    | potato | small 130, medium 170, large 300 | `:133-134` |
    | bell-pepper | medium 120, large 160 | `:135-136` |
    | lemon | medium 58 | `:139-140` |
    | lime | medium 67 | `:141-142` |
    | banana | small 90, medium 118, large 140 | `:143-144` |

  - `estimateGrams` ([ingredients.ts:96-119](../src/lib/ingredients.ts)) multiplies the count by these whenever the scanned unit is a size word. That's labelled 'estimated', but the number is still prefilled.
  - **Evidence they weren't measured:** a medium lime (67 g) is heavier than a medium lemon (58 g).
  - They're also inconsistent with the file's own rule that count weights are edible portion. PL-042 moves `lime` to flesh only (14-131), so any whole-lime weight would now be multiplied against flesh-only values.
- **Fix:** Undecided. Two options:
  - **Remove them.** Set `unit_grams` to `{}` for these eight via an `--only` run. A counted line then comes back `grams: null`, and the confirm step forces a weight, which is the designed safe state. The cost is one typed weight per counted produce line.
  - **Source them.** Take each weight from a citable UK portion source (FSA *Food Portion Sizes*; not on hand) on an edible-portion basis, and record the source per row.
  - Recommendation: remove now and reinstate row by row as sources are found. A sourced weight can come back in without a schema change.
  - Open: the same question applies to the other seeded values outside this list (egg sizes, garlic clove/bulb, the spoon weights, densities).
- **Where:** —
- **Shipped in:** —
- **Verified:** No.
- **Status:** Logged

---

## Deliberately not changed

| ID | Feedback | Why not |
|---|---|---|
| — | The other per-user selects in `src/` | Swept in the same pass; none is fixed here. Listed as candidates in PL-050's sweep table, not numbered |

---

## What went well

- The cap was found in review and measured before any account reached it. Raising hosted Max rows the same day bought about four months without a deploy.
